# AlphaLens 完整说明文档

> 面向第一届“工行杯”浙江大学金融科技创新大赛的项目总说明。
> 版本：AlphaLens-Demo-v2.0-R1 · 日期：2026-08-10
> **本报告仅供研究参考，不构成投资建议。**

---

## 1. 项目概览

| 项目 | 内容 |
|---|---|
| 项目名称 | AlphaLens（月度文本聚合行业同比 Nowcast 与量化研究智能体） |
| 一句话介绍 | 保留可审计 AI 与规则流水线，将同月去重文本聚合为月度特征；新文本实时处理后展示加入前后 Nowcast 与仓位的边际变化。 |
| 产品定位 | 量化研究助手 / 投研辅助工具，服务于因子挖掘、风险识别与研究报告生成。 |
| 重要边界 | **不是“AI 炒股”**——不让大模型直接预测股价涨跌，不输出买卖建议，不提供投资建议。 |

核心价值：把“新闻 → 情绪分数 → 个股因子”升级为“文本 → 事件/实体/19谓词 → 三层门控 → 同月去重聚合 → 月度文本残差 Ridge → 官方行业同比 Nowcast → 宏观预测差异配置”的可追溯链路。新文本只作为本月新增证据，不被包装成独立代表整个月。

## 2. 比赛背景

本项目参加**第一届“工行杯”浙江大学金融科技创新大赛**（校赛）。大赛聚焦金融科技创新，鼓励围绕真实金融场景提出有技术创新、应用价值和落地潜力的方案；优秀项目可能被推荐参加第五届中国研究生金融科技创新大赛、第十七届“工行杯”全国大学生金融科技创新大赛等后续赛事。

- **赛道形式**：自拟赛题赛道（与“揭榜挂帅赛道”并列）。
- **推荐方向**：开放创新服务（AlphaLens 对传统投研流程的重构更符合该方向）。
- **辅助贴合方向**：金融大模型与智能体、智能风控与量化建模、财富管理研究支持。
- **关键时间**：校赛报名截止 2026-08-17；作品提交截止 2026-08-31 15:00；校赛 2026 年 9 月上旬；国赛 2026 年 10 月中下旬。
- **内部目标**：2026-08-15 前完成可提交的校赛成果包。

## 3. 系统架构

```text
浏览器（研究界面）
  app/index.html + app/assets/（Plotly.js 图表、Lucide 图标）
        ↓ HTTP / JSON
Flask API（本地服务）
  app/server.py（8701 端口）
        ├── 模型网关与 Prompt        src/ai/          （gateway / prompts / research_layer / embeddings）
        ├── 实体、事件、谓词与门控    src/pipeline/    （link_entities / extract_events / ground_predicates / live_analysis）
        ├── 透明评分                  src/research/    （scoring）
        ├── 规则发现与回测            src/backtest/    （demo_engine）
        ├── 月度聚合Nowcast与兼容研究  src/macro/       （history / engine）
        ├── 外部数据安全导入          src/ingestion/   （text_import / common）
        └── 样例输入与研究产物        data/sample/     （33 个 CSV + 回放/人工抽检 JSON）
```

设计要点：前端是浏览器中的**研究界面**，后端仍是同一套 Python 研究函数；网页通过 API 调用，把中间证据、门控状态和审计信息结构化展示，而不是把终端画面搬进网页。

## 4. 核心研究链路

```text
新文本
→ 根据正文链接抓取 HTML/PDF 全文并评估来源与完整度
→ Embedding / 字符 n-gram 检索相似冻结规则与历史 AI 标注
→ DeepSeek V4 Flash 返回逐股票结构化 JSON
→ 事件类型与证据回溯校验
→ 股票池、关系证据与置信度校验
→ 每只股票完整 19 个谓词合法性校验
→ AI 与确定性程序逐项对照
→ 三层一致性门控
→ 冻结规则匹配与同类规则去重
→ 逐股票关系、谓词门控与冻结规则证据审计
→ 仅将本次文本的来源、事件、实体和19谓词转换成冻结模型特征
→ 电气机械和器材制造业增加值同比预测值与90%区间
→ 主要文本贡献和Markdown分析报告
```

与传统情绪因子的区别：

- **事件级理解**：不止看正负面，而是识别事件类型、影响主体、影响路径、证据强度和舆情扩散。
- **规则级归纳**：从历史样本自动发现“谓词组合 → 市场反应”的规则。
- **分层验收**：默认成果报告月度Nowcast、单篇文本加入前后边际变化、区间、验证误差和证据；金融策略研究单独验收，任一层不成立都不能包装成成功。

实时分析必须有大模型成功参与：缺少 Key、网络失败、模型身份不符、JSON 无法修复、事件或证据非法、没有逐股票 19 谓词时**直接报错**，不会退化成“假装 AI”的纯规则结果。断网时可走独立的“冻结回放”，回放始终标记 `is_replay=true`。

## 5. 模块详解

### 5.1 数据导入（src/ingestion/）
- `common.py`：原子写 CSV（临时文件 + `os.replace`）与 SHA-256 哈希工具。
- `text_import.py`：真实文本采集清单的严格校验与暂存。清单必须使用锁定七字段（`doc_id, source_type, title, content, publish_time, source_name, url`）；校验 `doc_id` 合法性/唯一性、来源类型、日期格式、URL 与去重、标题正文长度。默认只写 `data/external/` 暂存区，只有 `--apply` 才合并进 `raw_documents.csv`。

### 5.2 AI 研究层（src/ai/）
- `gateway.py`：自研的 OpenAI 兼容 HTTP 客户端（无 SDK 依赖），支持 JSON Schema / JSON Object 两种 `response_format`，失败时自动降级一次；区分 401/402/403/429 错误并给中文提示；`check_connection()` 同时核对 `/models` 权限列表与实际返回模型名（模型身份校验）。
- `prompts.py`：Prompt 版本 `alphalens-research-v2.1`，包含 9 种事件类型、19 个谓词定义、逐股票输出契约、来源限定事件类型与原文逐字证据修复 Prompt。
- `research_layer.py`：编排“检索 → 抽取 → 校验 → 修复一次 → 再校验”，对输出做严格领域校验（枚举、证据回溯、股票池、19 谓词完整性、辅助评分三项）；链接抓取成功时以抓取全文作为连续证据校验文本。
- `embeddings.py`：可选本地 BGE 中文向量（`BAAI/bge-small-zh-v1.5`），仅本地缓存加载，除非显式设置 `ALPHALENS_BGE_ALLOW_DOWNLOAD=1`；未安装时降级到自研确定性字符 2/3/4-gram 哈希向量，并在结果中如实标记真实后端。

### 5.3 文本处理管线（src/pipeline/）
- `link_entities.py`：先做精确名称/别名匹配，再按产业主题关键词映射，最后宽主题代表股映射；置信度随匹配方式递减。
- `extract_events_rule_based.py`：规则化事件抽取（政策利好、监管处罚、问询函、盈利质量、供应链、产品提价、产能扩张、关注扩散、投资者提问压力）；互动问答压力用“7 日窗口 ≥3 篇且相对 30 日基线倍增”的时间窗聚合判定。
- `ground_predicates_rule_based.py`：把事件逐条落地为 19 个谓词（含置信度与理由），两个数值谓词按 Beta 后验/事件证据强度赋值。
- `live_analysis.py`：实时新文本分析入口，串联三层门控、冻结规则匹配、因子计算与证据链输出。

### 5.4 透明评分（src/research/scoring.py）
见 §6.4；评分版本 `transparent-score-v2.0`。

### 5.5 规则发现与回测（src/backtest/demo_engine.py）
确定性、可复现的规则搜索与回测引擎，见 §6.6–6.8。

### 5.6 Web 服务与前端（app/）
- `server.py`：Flask 服务，API 见 §8；响应带 `no-store`；旧接口继续兼容，新增 `/api/macro/status`、`/api/macro/analyze`、`/api/macro/forecast`、`/api/macro/backtest`。
- `index.html` + `assets/`：默认首页提交一篇新文本；分析摘要展示加入前/后月度Nowcast、边际变化、预测区间和下一次调仓权重变化。逐股票关系、原文证据、19谓词和冻结规则在下方展开审计，结果末尾展示无同比信号、无文本同比模型、文本增强Nowcast三类收益。API Key只随请求传输、不落盘。

### 5.7 月度文本聚合 Nowcast（src/macro/）

- 主目标：国家统计局“电气机械和器材制造业增加值同比增速”。输入文本首次公开日对应的当期值必须尚未发布；官方目标发布日期严格晚于文本日期，1—2月保持一个合并观测。
- 当期特征：同月文本按来源URL或正文指纹去重，聚合来源、事件、实体、19谓词、规则、主题与质量特征；每个月只对应一条官方同比目标。
- 分区：2015—2021训练，2022—2023验证选择有限特征集、Ridge正则和文本残差权重；2024年起冻结OOS。
- 模型：无文本Ridge先根据最新已公布同比与季节项形成基线，文本Ridge学习该基线的月度残差。当前训练/验证为51/20个月，文本增强验证MAE为2.92pct，无文本基线为2.98pct，改善较弱；特征标准化值截尾至±3，文本增量按验证误差尺度设上限，防止OOS外推失控。
- 历史库：106篇官方全文，包括88篇国家统计局工业发布和18篇国家能源局、国家发展改革委、财政部等新能源政策；106/106抓取审计通过，共形成631条实体关联、626个事件、11,894条固定谓词。
- 报告：输出无文本预测、加入前/后Nowcast、本篇边际变化、90%区间、下一次仓位变化、主要结构化贡献与证据审计；尚未发生的持有期不生成收益。
- 策略：`516160` 与 `511010` 连续配置，不使用价格趋势、不做空、不加杠杆。当前10bp完整回测年化收益分别为：无同比信号-4.80%、无文本同比模型-6.80%、文本增强Nowcast-15.94%；文本增强未形成交易增量，如实披露负结果。

### 5.8 外部基准数据与可追溯性

- `macro_target_history.csv`：116 个国家统计局首次发布观测，覆盖 2015年1—2月至 2026年6月；每行保存官方发布页和发布日期。检索程序未可靠定位的 10 个月份保持缺口，不编造。
- `macro_historical_documents.csv`：106篇官方历史全文；`macro_historical_fetch_audit.csv`逐条记录抓取状态、发布日期、正文URL和接受状态，原 `raw_documents.csv` 不覆盖。
- `macro_market_data.csv`：新能源 ETF `516160` 来自东方财富历史行情接口，5年期国债 ETF `511010` 来自新浪财经历史行情接口；每行保存来源名称和接口 URL，证券代码为六位字符串且无交易所后缀。
- 所有新增 CSV 日期为 `YYYY-MM-DD`，布尔值为小写 `true/false`；既有锁定 CSV 表头不变，`raw_documents.csv` 重算前后仍由 SHA-256 强制保护。

### 5.9 入口脚本
| 脚本 | 用途 |
|---|---|
| `启动演示.py` | 统一启动入口，运行 `app/server.py`（端口 8701） |
| `运行研究流水线.py` | 只重建派生数据；前后强制比对 `raw_documents.csv` 的 SHA-256，防止覆盖人工核验文本 |
| `导入真实文本.py` | 默认暂存、`--apply` 才合并的真实文本导入 |
| `采集Discovery互动问答候选.py` | 仅从深交所互动易公开接口暂存 2024-2025 候选，列表与详情时间双重校验、哈希去重；不修改 raw |
| `更新真实行情.py` | AkShare 拉取前复权日线，默认暂存、`--apply` 才替换 |
| `批量生成AI标注.py` | 逐篇调用 DeepSeek 生成可恢复、可审计的历史标注缓存 |
| `准备本地向量模型.py` | 显式准备 BGE 本地模型 |
| `检查数据覆盖.py` | 输出“分区 × 来源 × 事件类型”覆盖缺口与待补清单，返回码 0/1 可接入流水线 |

## 6. 关键技术细节

### 6.1 19 谓词锁定 Schema
| 类别 | 谓词 |
|---|---|
| 政策 | `has_policy_support`、`policy_directly_related_to_business`、`demand_side_policy`、`supply_side_policy`、`capacity_policy_support`、`policy_attention_followup` |
| 来源 | `evidence_from_authoritative_source`、`source_government_or_exchange`、`source_company_announcement`、`source_major_media` |
| 关注度 | `social_attention_spikes`、`institutional_attention_increases`、`investor_questions_increase` |
| 风险 | `management_response_vague`、`announcement_contains_uncertainty`、`risk_or_uncertainty_disclosure` |
| 产品 | `event_mentions_core_product` |
| 数值 | `event_evidence_strength`、`event_has_short_term_price_impact` |

每只股票必须返回完整、无重复的 19 个谓词；缺项直接判失败并触发一次修复。

### 6.2 三层一致性门控
1. **事件门控**：确定性程序与 AI 识别的事件类型必须一致；规则未覆盖但 AI 枚举合法且证据可回溯时为 `ai_validated`；冲突则事件被拒。
2. **实体关系门控**：股票代码必须在锁定股票池内；AI 关系证据必须是原文连续子串，关系置信度 ≥ 0.50。
3. **谓词门控**：`agreed_true`（AI 与确定性均为真）才可触发冻结规则；`agreed_false` / `disputed` / `invalid` 均不进入规则。冲突融合值仅用于界面展示、置信度分析和 AI 候选研究，不参与冻结规则匹配。两个数值谓词差值 ≤ 0.10 视为一致，且只参与因子倍率、不当布尔条件。

### 6.3 实时技术路线中的模型身份
调用模型固定为 `deepseek-v4-flash`，基础地址固定为 `https://api.deepseek.com`。`POST /api/ai/check` 分两步：先查 `/models` 确认 Key 拥有该模型权限并记录 `owned_by`，再发最小聊天请求核对**实际返回模型名**必须与请求完全一致；返回其他模型（如 Pro）时拒绝结果。

### 6.4 透明证据评分
```text
E = 0.30 × 来源可靠性 + 0.25 × 证据回溯 + 0.25 × 信息具体性 + 0.20 × 业务关联
```
- 来源可靠性：政策/公告 0.95；互动问答 0.85；主流财经媒体 0.80；其他新闻 0.60。
- 证据回溯：证据句能在原文连续定位为 1，否则 0。
- 信息具体性：行动、明确主体、量化事实、日期/项目事实四项等权。
- 业务关联：公司名直接出现 1；产业主题映射 0.80；宽主题代表股 0.60。
- AI 只能校正后三项，最终每项取“确定性结果与 AI 结果的较小值”，因此 AI 不能把分数抬高。

### 6.5 数值影响谓词（Beta 后验）
`event_has_short_term_price_impact` 不使用固定值。系统只用 Discovery 数据，按独立 `doc_id` 聚合行业超额收益，统计绝对 5 日超额 ≥ 2% 的文档数，用 Beta(2,2) 先验：

```text
影响后验概率 = (命中文档数 + 2) / (独立文档数 + 4)
```

结果写入 `event_type_impact_priors.csv`，表示“历史同类事件出现较大短期波动的后验频率”，不代表上涨概率或收益预测。

### 6.6 规则发现与透明评分
规则只使用 2024-01-01 至 2025-12-31 的 Discovery 数据；支持度按**独立文档**计算（一篇行业政策映射多只股票仍只计 1 篇），同时记录独立日期与股票覆盖；事件集合高度重复的规则只保留最高分，同一目标标签也只保留最高分规则。

```text
S = 0.30 × Beta 后验胜率 + 0.25 × 收缩收益分位 + 0.20 × 半年稳定性
  + 0.15 × 文档/股票覆盖 + 0.10 × 平均证据分 - 0.03 × (谓词项数 - 1)
```
- 后验胜率使用 Beta(2,2)；平均收益按样本量向全局 Discovery 均值收缩后再经 Logistic 转 0–1；半年稳定性为正收益半年占比；覆盖项由最多 20 篇文档与 10 只股票构成。
- 全部规则分项写入 `rule_diagnostics.csv` 并在审计页展示。

### 6.7 因子公式
```text
候选因子 = 门控后冻结规则评分和 × (0.7 × 透明证据分 + 0.3 × 同类事件影响后验)
```
事件因子先聚合到唯一“交易日 + 股票”，再按行业做截面标准化。候选因子只表示当前研究规则对文本证据的响应强度，不是收益预测或买卖信号。

### 6.8 历史回测口径
- Discovery：2024-01-01 至 2025-12-31，只用于发现与评分。
- OOS：2026-01-01 至 2026-06-30，只用于样本外检验。
- 收益：入场日开盘至第 5 个交易日收盘，减同日行业等权收益。
- 五组：每个交易日独立按横截面因子排序分组，不全局排序事件。
- Rank IC：每日因子排序与 5 日行业超额收益排序的 Spearman 相关；ICIR = 均值 / 标准差。
- 未来函数：入场交易日必须严格晚于 `event_time`（`future_info_audit` 校验通过）。
- 诚实披露：当前 OOS 有效 IC 日过少，页面明确判定“证据不足”，既不能证明稳定有效，也不断言已失效。

### 6.9 真实数据安全导入
- 文本：七字段锁定的采集清单 → 暂存区校验报告 → `--apply` 才按 `doc_id` 合并。
- 行情：AkShare 拉取股票池 30 只前复权日线，全部成功才写入暂存；`--apply` 才替换 `market_data.csv`。
- 复权口径：价格列使用前复权；`adj_factor=1` 是占位字段，不是真实复权因子序列，在审计页持续披露。

## 7. 数据说明

### 7.1 数据文件（data/sample/）
| 文件 | 内容 | 规模 |
|---|---|---|
| `stock_pool.csv` | 锁定股票池 | 30 只（光伏/锂电/风电/储能/整车） |
| `raw_documents.csv` | 人工核验原始文本 | 196 篇（政策 55 / 新闻 56 / 公告 55 / 互动问答 30） |
| `entity_links.csv` | 文档-股票实体链接 | 以当前样例 CSV 为准 |
| `events.csv` | 结构化事件 | 349 个 |
| `predicates.csv` | 事件谓词（19/事件） | 4066 条 |
| `predicate_matrix.csv` | 事件-谓词矩阵 | 349 行 |
| `event_forward_returns.csv` | 未来 5/10 日收益 | 343 条 |
| `event_type_impact_priors.csv` | 事件类型影响后验 | 以当前 Discovery 事件类型为准 |
| `rules.csv` | 候选/合格规则 | 14 条（合格 5） |
| `rule_diagnostics.csv` | 规则透明评分分项 | 14 行 |
| `factors.csv` | 事件因子 | 57 条 |
| `factor_snapshot.csv` | 最近截面因子快照 | 30 行 |
| `group_returns.csv` / `group_returns_by_split.csv` | 五组收益 | 5 行 / 10 行 |
| `rank_ic_timeseries.csv` | 每日 Rank IC | 以当前重算结果为准（OOS 有效截面 13 个） |
| `backtest_metrics.csv` / `backtest_metrics_by_split.csv` | 回测指标 | 以当前重算结果为准 |
| `market_data.csv` | 前复权行情 | 18002 行（2024-01-02 至 2026-06-30） |
| `replay_cases.json` | 冻结回放案例 | 1 个（storage-policy） |
| `manual_review_summary.json` | 人工抽检汇总 | 事件 10 条 / 谓词 0 条 |

### 7.2 样本分布现状（审计页可查）
| 分区 | policy | announcement | news | ir_qa |
|---|---|---|---|---|
| Discovery | 30 / 25 ✅ | 27 / 25 ✅ | 31 / 25 ✅ | 0 / 25 ⚠️ |
| OOS | 25 / 25 ✅ | 28 / 25 ✅ | 25 / 25 ✅ | 30 / 25 ✅ |

存在明显的“分区 × 来源类型”不平衡：Discovery 互动问答仍为 0/25；其余 Discovery/OOS 来源已达到每格25篇门槛。系统在审计页如实展示待补数量，**不通过复制文档虚增独立支持**。

Discovery 互动问答使用 `采集Discovery互动问答候选.py` 的非破坏性暂存流程：调用深交所互动易公开接口后，仍以列表返回时间和详情返回时间进行双重分区校验，校验股票代码、唯一 `questionId`、详情页 URL、提问/回复完整性和问答内容哈希；通过者也只能进入 `data/external/` 的人工核验候选表，不能直接写入 `raw_documents.csv`。2026-08-10 对股票池前 3 只股票的小范围联网验收得到 0 条 2024–2025 合格候选，其中 002129 的 10 条返回记录均被实际日期过滤拒绝，故缺口仍如实保留为 0/25；接口日期参数本身不被视为历史归档证明。为使提交包不依赖被排除的 `data/external/`，空白的受控人工核验清单位于 `采集模板/Discovery互动问答采集模板.csv`。

### 7.3 事件类型分布
现有事件已覆盖 9 种类型：`policy_support`(153)、`attention_spread`(89)、`capacity_expansion`(38)、`earnings_quality_anomaly`(29)、`regulatory_penalty`(18)、`supply_chain_disruption`(12)、`inquiry_letter_pressure`(5)、`investor_question_pressure`(4)、`product_price_increase`(1)。事件覆盖不等同于收益有效性，仍需遵守 Discovery/OOS 分区与证据门槛。

## 8. API 一览

| 方法 | 路径 | 用途 |
|---|---|---|
| `GET` | `/` | 前端页面 |
| `GET` | `/assets/<file>`、`/vendor/<file>` | 前端静态资源 |
| `GET` | `/api/status` | 数据统计、模型状态、版本与免责声明 |
| `GET` | `/api/backtest` | Discovery/OOS 回测指标、五组收益、Rank IC、合格规则与审计说明 |
| `GET` | `/api/audit` | 数据分布、分区覆盖、人工抽检、历史 AI 严格拒绝分类、模型/评分/规则/代码版本、未来函数审计 |
| `GET` | `/api/ai/status` | AI 层配置状态 |
| `POST` | `/api/ai/check` | Key / 模型列表 / 实际返回模型身份检查 |
| `POST` | `/api/analyze` | 新文本 AI 分析与月度Nowcast边际变化（hybrid 模式，无 Key时拒绝） |
| `GET` | `/api/macro/status` | 月度聚合模型与验证状态 |
| `POST` | `/api/macro/analyze` | 新文本 AI 全链路、同比预测与报告 |
| `GET` | `/api/macro/forecast` | 历史目标与兼容研究序列 |
| `GET` | `/api/macro/backtest` | 兼容保留的策略研究结果 |
| `GET` | `/api/replay/<case_id>` | 冻结回放（强制 `is_replay=true`） |

分析响应包含事件、实体、谓词门控审计，并附带 `text_forecast` 和 Markdown 报告。`text_forecast.forecast_mode=monthly_nowcast_marginal_text`，返回无文本预测、加入前/后Nowcast、边际变化、仓位变化和“未来收益尚不可观察”状态。

2026-08-10 已使用真实 DeepSeek Key 对本地服务执行一次非持久化验收：`/api/ai/check` 的 `/models`、请求/返回模型、`request_id` 与 `system_fingerprint` 一致；随后政策链接全文的 `/api/analyze` 返回 HTTP 200，接受 5 只关联股票和完整 19 谓词 Schema。该样本的三层共识门控为未通过，系统如实不触发冻结规则。请求 Key 仅随当次请求使用，检查响应确认 `credential_retained=false`；实时 AI 候选规则只展示，不写入 `data/sample/`。

## 9. 启动、验证与安全重算

```bash
# 启动（Python 3.9+）
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
.venv/bin/python 启动演示.py        # 浏览器打开 http://127.0.0.1:8701

# 验证
.venv/bin/python -m unittest discover -s tests -v     # 53 项自动化验收测试（v2.0-R1 实测）
.venv/bin/python -m compileall -q app src tests 运行研究流水线.py

# 安全重算（只重建派生数据，保护 raw_documents.csv）
.venv/bin/python 运行研究流水线.py
```

经本轮实测，唯一允许的流水线成功重建33个样例CSV；`raw_documents.csv` 重算前后 SHA-256 均为 `9af278fa743daa7a109ad46ff923bd4bb13603d6b3794b4b2768f6b6d5852179`，人工核验成果未被覆盖。

## 10. 可选能力

```bash
# 本地 BGE 语义检索（未安装/未缓存时自动降级到字符 n-gram）
.venv/bin/pip install -r requirements-ai.txt
ALPHALENS_BGE_ALLOW_DOWNLOAD=1 .venv/bin/python 准备本地向量模型.py

# 历史 DeepSeek 标注缓存
DEEPSEEK_API_KEY=你的Key .venv/bin/python 批量生成AI标注.py --limit 10

# 真实文本与行情（默认只写 data/external/ 暂存区）
.venv/bin/python 导入真实文本.py 核验后的采集清单.csv [--apply]
.venv/bin/pip install -r requirements-data.txt
.venv/bin/python 更新真实行情.py --start 2024-01-01 --end 2026-06-30 [--apply]
```

## 11. 第三方依赖、合规与原创性

- **运行时**：Python 3.9+（PSF）、Flask 3.x（BSD-3）、Plotly.js（MIT，本地 vendor 文件）、Lucide 0.468（ISC，本地 vendor 文件）。
- **外部服务**：DeepSeek API（模型 `deepseek-v4-flash`）。
- **可选**：sentence-transformers（Apache-2.0）、BAAI/bge-small-zh-v1.5（MIT）、AkShare（MIT）。
- **自研部分**：字符 n-gram 检索、JSON 合法性校验与一致性门控、实体链接/事件抽取/谓词落地、Beta 后验与规则透明评分、行业超额收益与五组回测、Flask API 与前端研究界面。
- **数据与安全**：API Key 不写入仓库/CSV/日志/浏览器存储；实时请求会把当次输入文本发送到 DeepSeek，演示应使用可公开文本；冻结回放不联网且强制标记。
- 工行杯可演示成果将产品、技术、提交与原创性说明统一合并到 README，不再保留独立“原创性证明”文件。

## 12. 已知限制与诚实披露

1. 当前 OOS 有效 IC 日过少、因子活跃日稀疏，**证据不足**，不能宣称因子稳定有效。
2. 样本存在“分区 × 来源类型”失衡，Discovery 互动问答仍为 0/25；不能复制文本虚增支持度。
3. 合格冻结规则仅5条，支持度仍有限；新增2015—2023历史文本使用确定性事件/谓词处理，不冒充历史 DeepSeek 调用结果。
4. `adj_factor=1` 是占位字段，不是真实复权因子序列。
5. 兼容策略层报告5/10/20bp成本且不做空、不加杠杆，但原个股5日因子研究仍未完整模拟流动性、冲击成本和容量约束。
6. 历史 AI 标注缓存当前为 163/196 成功、33 条严格拒绝；拒绝原因和不放宽校验的修复策略见 `AI标注拒绝分析.md`。真实 Key 验收后可用 `--retry-failed` 仅重试失败文档。
7. 历史 AI 缓存字段 `generated_at` 依赖系统时钟，回放 `usage` 为空属正常；重试缓存为 append-only，审计按每篇文档最新记录统计。

## 13. 目录结构与归档说明

```text
工行杯/
├── 比赛提交/GitCode_AlphaLens/  ← 保留的完整可运行项目源码
│   ├── app/                     前端 + Flask API
│   ├── src/                     全部研究源码
│   ├── data/sample/             演示数据与产物
│   ├── tests/                   验收测试
│   ├── VERSION                  版本标签（无 git 目录时审计页兜底）
│   ├── 检查数据覆盖.py          覆盖缺口清单工具
│   ├── 完整说明文档.md           本文档
│   ├── README.md                快速上手
│   └── 各入口脚本
└── 项目历史资料/                ← 归档（从 Git f30b9a2 恢复，非提交包）
    ├── 参考文档/                赛题说明、Schema、Prompt、数据契约
    ├── 研发脚本/                数据获取、核验、校验脚本
    ├── 阶段材料/                质量报告、抽检表、答辩/审计材料
    ├── 自动测试/                旧版 Playwright + Python 测试
    ├── 旧版演示资料/            旧版说明、截图、配置示例
    └── 归档说明.md
```

`项目历史资料/` 仅用于研发追溯、测试复现和团队查阅，不属于最终提交包；历史脚本可能引用旧目录结构，不应覆盖当前 `data/sample/`。

---

**AlphaLens 是量化研究助手，不预测股票价格，不提供买卖建议。**
