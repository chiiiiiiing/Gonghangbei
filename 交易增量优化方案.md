# AlphaLens 交易增量优化方案

> 版本：lithium-v6-quality-text-walkforward · 更新日期：2026-08-15
> 本报告仅供研究参考，不构成投资建议。

## 1. 现状结论

当前项目已经完成从“月度行业 Nowcast”到“碳酸锂 5 日方向 + 成熟市场基准 + 质量规则文本 Alpha”的主链路切换。冻结的 V5 候选已经证明历史模拟中的可归因交易增量：

| 口径 | V5 质量规则 Alpha |
|---|---:|
| 2025+ 历史滚动年化收益差 | +5.48% |
| 2025+ Bootstrap 95% 下界 | +1.46% |
| 2026 压力段年化收益差 | +5.59% |
| 2026 Bootstrap 95% 下界 | +0.14% |
| 2/5/10 bp 成本敏感性 | 通过 |

但严格前瞻样本只有 1 条、尚未结算，因此系统仍不能宣称“严格前瞻交易增量已建立”。这是当前唯一的硬约束，不能通过回填旧 OOS 解决，只能持续积累未来交易日。

## 2. 主要瓶颈

1. **文本信号偏弱且混杂**：V5 只要求 `zero_shot_score > 0`，低置信度、低强度和带高不确定性的样本仍会进入 Alpha。
2. **文本权重偏保守**：0.25 权重下，单条高质量信号产生的边际仓位较小。
3. **严格前瞻证据不足**：append-only 账本尚未积累到 63 个已结算交易日。
4. **日常更新链路缺少 V6 运行审计**：V5 有前瞻账本，但新候选需要独立的完整性校验和运行日志。

## 3. 已实施优化

### 3.1 新增 V6 优化候选

在不修改 V5 冻结源码和账本的前提下，新增独立的 V6 候选：

- 质量门禁：`authoritative_source AND non_GFEX AND zero_shot_score >= 0.2 AND NOT uncertainty_high`
- 文本 Alpha 权重：0.35
- 成熟市场基准、20/60/120 日动量、25% 年化波动率目标与 V5 完全一致
- 无合格文本时增强仓位严格等于基准，保持可归因

历史 walk-forward 结果：

| 口径 | V6 优化候选 | V5 对照 |
|---|---:|---:|
| 2025+ 历史滚动年化收益差 | +7.50% | +5.48% |
| 2025+ Bootstrap 95% 下界 | +2.54% | +1.46% |
| 2026 压力段年化收益差 | +9.30% | +5.59% |
| 2026 Bootstrap 95% 下界 | +2.68% | +0.14% |
| 通过质量规则信号数 | 40 / 259 | 72 / 259 |

### 3.2 独立前瞻账本与冻结校验

- 新增 `lithium_v6_prospective_decisions.csv`，append-only，重复或字段变化拒绝写入。
- 新增 `lithium_v6_candidate_freeze.json`，冻结源码、输入前缀、报告和账本哈希。
- 新增 `scripts/update_lithium_v6_prospective.py`，每日 17:00 后推进官方行情并追加 V6 决策与运行日志。
- 新增 `scripts/collect_live_cninfo_lithium_signals.py`，抓取最近巨潮资讯锂产业链公告并写入非交易所前瞻信号；否则 V6 前瞻账本每天只有基准决策，文本 Alpha 恒为 0。
- 新增 `scripts/run_daily_lithium_v6.py`，统一编排实时信号采集、官方行情更新、V6 决策追加和监控输出，生产环境 cron 只需一条命令。
- 新增 `scripts/verify_lithium_v6_system.py`，一次运行 V5/V6 冻结完整性校验和 V6 状态 smoke check。
- 验证脚本会生成 `data/research/lithium_v6_system_state.json`，作为当前系统状态的可审计快照。
- 新增 `src/lithium/walkforward_v6.py`，保持 V5 模块不变。

### 3.3 Web/API 与界面

- `GET /api/lithium/research-v6` 返回 V6 报告与前瞻状态。
- `GET /api/lithium/monitor-v6` 返回严格验收进度：已结算数、剩余结算天数、Bootstrap 下界和是否可最终验收。
- `/api/lithium/status`、`/api/lithium/backtest` 同时返回 V5 与 V6。
- `/api/lithium/analyze` 额外返回 `v6_strategy_mapping` 和 V6 增量证据。
- 研究验证页新增 V6 优化候选卡片，实时分析页显示 V6 增强仓位与边际变化。

## 4. 为什么这是“整个系统”的优化

- **研究层**：V6 通过更高门槛减少噪声，提升历史 walk-forward 增量。
- **数据层**：V5 和 V6 共用已审计的广期所行情、仓单和产业文本，不新增未审计数据。
- **策略层**：V6 与 V5 使用同一成熟市场基准，只调整文本质量门禁和边际权重，收益差仍可归因。
- **运行层**：V6 拥有独立冻结清单、前瞻账本、每日更新脚本和运行日志，保证后续 63 个交易日可以持续自动结算。
- **展示层**：API 和前端同时呈现 V5/V6，不把优化候选伪装成已成立结论。

## 5. 仍然不能做的

V6 历史结果是在 2026-08-15 候选形成时已经可见的历史数据，不能冒充新的未见 OOS。严格交易增量必须满足：

1. 从 2026-08-15 后的 append-only 前瞻账本积累至少 63 个已结算交易日；
2. 成本后年化收益差为正；
3. 3 个月时间块 Bootstrap 95% 下界大于 0。

在满足前，系统必须持续显示“严格前瞻交易增量待检验”。

## 6. 后续行动

1. 每个交易日 17:00 后运行 V6 更新脚本，并确认 V4/V5/V6 完整性校验均通过。
2. 使用 `scripts/monitor_lithium_v6.py` 检查已结算数、待结算数和 bootstrap 观测数；该脚本在达到严格门槛前返回非零，适合接入告警。
3. 每天检查 `lithium_v6_prospective_runs.csv` 中的结算数、待结算数和 bootstrap 观测数。
4. 当 V6 严格账本达到 63 个结算样本后，用同一门槛做最终验收。
5. 若 V6 严格账本不通过，回退 V5 已冻结候选，不根据 V6 历史结果再次调参。

本报告仅供研究参考，不构成投资建议。
