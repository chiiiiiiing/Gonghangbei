# AlphaLens 第三方依赖说明

版本日期：2026-08-04

## 运行时依赖

| 依赖 | 用途 | 授权与来源 |
|---|---|---|
| Python 3.9+ | 服务端、数据处理和回测 | Python Software Foundation License |
| Flask 3.x | 本地 HTTP API 和静态页面服务 | BSD-3-Clause，`requirements.txt` |
| Plotly.js | 历史分组收益和 Rank IC 图表 | MIT License，本地文件 `app/vendor/plotly.min.js` |
| Lucide 0.468 | 前端按钮图标 | ISC License，本地文件 `app/vendor/lucide.min.js` |
| DeepSeek API | 结构化事件、谓词和候选规则 | 外部模型服务，模型 `deepseek-v4-flash` |
| sentence-transformers（可选） | 本地 BGE 中文语义向量 | Apache-2.0，`requirements-ai.txt` |
| BAAI/bge-small-zh-v1.5（可选） | 中文文本向量模型 | MIT License，用户显式下载到本地模型缓存 |
| AkShare（可选） | 公开 A 股前复权日线采集 | MIT License，`requirements-data.txt` |

## 自研部分

以下模块为项目组自主实现：

- 字符 n-gram 降级向量和冻结规则检索编排。
- 模型 JSON 输出合法性校验与 AI/确定性谓词一致性门控。
- 实体链接、事件抽取、时间窗口聚合、逐股票谓词落地与透明证据评分。
- Beta 后验影响概率、规则透明评分、行业超额收益、按日五组回测和 Rank IC。
- Flask API、前端研究工作区、审计页和自动研究记录。

## 数据与安全

- API Key 不写入仓库、CSV、日志或浏览器存储。
- `data/raw/`、`data/processed/`、`data/external/` 不提交 Git。
- 实时 DeepSeek 请求会把用户当次输入文本发送到 DeepSeek API；演示时应使用可公开文本。
- 冻结回放不连接外部模型，且页面强制标记回放状态。

**本报告仅供研究参考，不构成投资建议**
