# AlphaLens 问题与待改进报告

> 基于 2026-08-05 全量代码分析 + 自动测试 + 流水线可复现性校验所得。
> **本报告仅供研究参考，不构成投资建议。**

## 变更记录（2026-08-05 优化轮）

本轮已修复（见各节详情）：

- ✅ **2.1 模型配置三处不一致** → 已统一为 DeepSeek（`gateway.py` 默认值、前端状态栏、审计页模型字段）。
- ✅ **2.2 `repository_commit` 依赖 git** → 新增 `VERSION` 文件兜底，且捕获 `git` 二进制缺失（`FileNotFoundError`），脱 git 或未装 git 时均显示版本标签。
- 🔶 **2.3 源文本含“项目关联：”注释** → **评估后决定保留（团队决策）**：该注释是逐条溯源/免责标记，代码在所有证据回溯、抽取、评分路径都用 `split("项目关联：", 1)[0]` 剥离，且**从不泄漏到界面**；`raw_documents.csv` 是人工核验受保护输入，改动需团队手工确认（`sed` 会破坏带引号字段，须用 CSV 解析改写）。如需纯净化，团队可用已验证清单经 `导入真实文本.py` 重建。
- ✅ **2.4 证据文本硬截断** → 改为按句子边界截断（`_truncate_at_boundary`），并新增测试。
- ✅ **3.2 行情空表保护** → `research_audit` 对 `min/max` 加 `default`，`adj_factor_placeholder` 空表不再误报。
- ✅ **3.3 指标转换脆弱性** → 新增 `int_metric()` 健壮转换，`decay_assessment` 不再依赖字段格式。
- ✅ **3.5 前向收益缺失静默跳过** → 增加跳过事件日志。
- ✅ **3.6 测试补充** → 新增 DeepSeek 默认配置、句子边界截断两项测试。
- ✅ **1.2/1.3 数据覆盖任务工具化** → 新增 `检查数据覆盖.py`：把“分区 × 来源 × 事件类型”缺口变成可量化待补清单（返回码 0/1 可接入流水线），团队只需按清单采集并用 `导入真实文本.py` 入库。
- ✅ **3.7 答辩 PPT 悬空删除** → 已从 git 恢复 `AlphaLens演示答辩.pptx`。
- 🔶 **1.1 OOS 证据充分性** → 本质是样本量问题（非代码缺陷）。系统如实披露"证据不足"是**设计行为**；已用 `检查数据覆盖.py` 明确缺口（OOS 缺政策 20 / 新闻 12 篇），补足后重算即可实质改善，属可执行的采集任务。
- 🔶 **1.4 合格规则偏少** → 根因是 Discovery 样本稀疏；随 1.2 补样自然改善，规则门槛（`MIN_*` 阈值）属于有意的研究口径，不建议为凑数而放宽。
- ⚠️ **历史 AI 标注缓存** → 需 DeepSeek Key 运行 `批量生成AI标注.py`，属一次性外部凭证任务。
- ✅ **2.5 `/api/ai/status`** → 保留端点但已通过统一配置保持一致（不再与 `/api/status` 矛盾）。

---

## 变更记录（2026-08-06 数据补全轮）

本轮是**可执行的数据补全**（非代码缺陷），已完成：

- ✅ **覆盖缺口 8 → 1 项**：OOS 政策 5→25、OOS 新闻 13→25、discovery 新闻 22→31、discovery 公告 2→27；文档总数 130→196，事件 214→349。仅剩 discovery/互动问答 0/25。
- ✅ **9 类事件类型全部有样本**：补 regulatory_penalty（立案调查/行政处罚，18 事件）、product_price_increase（价格上调，1 事件）、supply_chain_disruption（停产/生产事故，12 事件）三类此前零样本类型。
- ✅ **OOS 证据 0 → 13 个有效 IC 截面**：因子样本 5→20、活跃因子交易日 1→13、合格规则 2→5。`evidence_status` 仍为 `insufficient`（阈值：IC 截面 ≥20 且事件样本 ≥50），为诚实披露的设计行为。
- ✅ **新增 3 个采集工具**（已编译通过，测试 20/20 保持）：`预演导入.py`（只读预演链接/事件/缺口/去重）、`采集正文.py`（curl 抓网页正文回填，含编码回退与句子边界截断）、`采集公告.py`（巨潮 API + pypdf 抓股票池公告，按事件关键词过滤）。
- 🔶 **补数经验（重要，写入待办）**：OOS 有效截面对「多板块同日事件」敏感。实测新增整车政策 3 篇、产能类公告 13 篇均使 OOS IC 截面不升反降（13→7、13→6），已回退。进一步改善须补「跨板块、同日期」的 2026 Q1-Q2 OOS 政策/新闻。
- 🔶 **互动问答格缺口**：互动易（irm.cninfo.com.cn）与上证e互动（sns.sseinfo.com）API 均被 405 拦截、JS 渲染无法程序化抓取，WebSearch 无索引详情页。已生成采集模板与说明（`data/external/文本导入暂存/互动问答采集模板.csv`），待团队浏览器手动采集 25 条。
- ⚠️ **OOS 窗口口径**：维持 2026-01-01 起（口径干净、零披露负担）。补样本后 OOS 仍不足，按交接口径如实披露，不调阈值。

---

## 0. 测试结论摘要

| 检查项 | 结果 |
|---|---|
| 单元测试（`tests/test_alphalens.py`） | ✅ 18/18 通过 |
| 语法编译（`compileall app src tests 运行研究流水线.py`） | ✅ 全部通过 |
| 演示服务启动（`启动演示.py` → 8701） | ✅ 正常 |
| 首页 / 静态资源 / vendor 资源 | ✅ 全部 HTTP 200 |
| `/api/status`、`/api/backtest`、`/api/audit` | ✅ 200，字段完整 |
| `/api/replay/storage-policy` | ✅ 200，`is_replay=true`，门控通过 |
| `/api/analyze` 无 Key / 非法参数 / 未来日期 | ✅ 分别返回 503/400/400 |
| 假 API Key（真实 401） | ✅ 正确捕获并映射中文错误，返回 503 |
| 冻结回放 404 / `/api/ai/check` 无 Key | ✅ 404 / 400 |
| **流水线可复现性** | ✅ 重算后 18 个 CSV 与提交版**逐文件完全一致** |
| 原始文本保护（SHA-256） | ✅ 重算前后 `raw_documents.csv` 哈希不变 |
| API Key 泄漏检查 | ✅ 日志中无 Key |
| 谓词完整性 | ✅ 214 事件 × 19 谓词，无缺失/孤儿 |
| 前端 `app.js` 语法 | ✅ `node --check` 通过 |

**总体结论：工程与数据完整性非常好——门控、审计、输入保护、诚实披露都做到了位，且可复现。** 主要短板集中在**研究证据的充分性**与**若干展示一致性**上，见下文。

---

## 0.1 因子收益能力实测（2026-08-05 复核）

> 直接用引擎同一套行业中性口径，对 213 条事件收益做了显著性检验与组合模拟。**结论：当前数据下因子尚不能证明能赚钱，但存在一个"排名信号"的雏形。**

### 事件级收益（5 日持有）

| 口径 | Discovery | OOS |
|---|---|---|
| 原始收益（未中性化） | +1.41%，t=2.77 ✅ | +0.37%，t=0.41 |
| 行业中性超额收益 | −0.08%，t=−0.22 | −0.14%，t=−0.21 |
| 超额正收益比例 | 56% | 42% |

**关键解读**：Discovery 事件股票 5 日平均涨 +1.41% 且统计显著，**但这是行业贝塔，不是事件 alpha**——做完行业等权中性化后平均超额只剩 −0.08%（不显著）。即：事件股确实涨了，但它所在行业（新能源/光伏/汽车/储能 2024—2025 强势板块）涨得一样多甚至更多。若答辩只讲"事件后股票涨了 1.41%"会被立刻用"这是板块行情"反驳；系统坚持用行业中性超额，是**正确且诚实**的口径。

### 横截面排名信号（Discovery）

- 6 个有效 IC 截面：平均 Rank IC **+0.047**，ICIR +0.197，t=+0.48（**不显著**）。
- 6 期中 **5 期为正**（−0.42，+0.02，+0.16，+0.14，+0.16，+0.23），唯一负值是大单期离群。
- 五组 G5−G1 行业超额差价 **+0.22%**/5 日（G1 −0.13%，G5 +0.09%），方向单调。
- **能赚钱吗？现在不能。** 事件驱动等权长多组合 Discovery 累计 −25%（年化约 −9%），OOS −14%。唯一可能成立的用法是"多空排名"（做多 G5 做空 G1），+0.22%/5 日约年化 +11%——但基于仅 6 个截面，统计上不可分辩于噪声，且 OOS 因横截面退化（5 只整车股同因子值）**零有效 IC 日，无任何证据**。

### 对答辩的含义

1. 因子当前定位应为**"研究素材/排序信号"，不是"能赚钱的策略"**；回测闭环如实展示、不夸大。
2. +1.41% vs −0.08% 的对照是很好的**方法学展示**：系统能区分行业贝塔与真实 alpha。
3. 想证明能赚钱，唯一路径仍是**补样本**（`检查数据覆盖.py` 清单）→ 重算 → 让有效 IC 截面从 6 个提升到 20+，OOS 截面从 0 提升到可用。

---

## 1. 严重问题（影响研究可信度 / 比赛评分）

### 1.1 OOS 证据不足，回测说服力弱
- **现状**：OOS 仅 5 个因子样本、1 个活跃因子日、**0 个有效 IC 日**；`avg_rank_ic_5d=0`、`rank_ic_ir=0`、`top_bottom_group_spread_5d=0`，证据状态为 `insufficient`。
- **影响**：这是本项目最大短板。答辩时“样本外检验”无法给出任何有区分度的数字，评委容易质疑“回测闭环是否真的成立”。
- **根因**：OOS（2026-01-01 至 2026-06-30）内被事件触发的股票日太少；规则触发稀疏（见 1.4）。
- **建议**：
  - 扩充 OOS 期政策/新闻样本文本（当前分别只有 5/13 篇，见 1.2），使更多股票日形成因子截面。
  - 若时间允许，将 OOS 窗口向前扩展（如从 2025-10-01 起），换取更多 OOS 交易日——但**必须保证 Discovery/OOS 不重叠**，且窗口调整要在文档中如实说明。
  - 在证据不足时，把“为什么 OOS 样本不足”作为答辩主动披露点，比被追问更好。

### 1.2 样本“分区 × 来源类型”严重失衡
- **现状**（审计页可查）：Discovery 缺公告（2/25）与互动问答（0/25），OOS 缺政策（5/25）与新闻（13/25）。
- **影响**：规则只用 Discovery 归纳，而 Discovery 的政策/新闻占比过高、公告/互动问答几乎为空，导致规则发现偏科；OOS 又反过来缺政策/新闻，无法对同类型规则做公平的样本外检验。
- **建议**：按审计页“待补”清单，用 `导入真实文本.py`（暂存 → 校验 → `--apply`）补齐四格缺口，每格到 25 篇。这是性价比最高的改进。

### 1.3 事件类型覆盖不全
- **现状**：9 种事件类型只覆盖 6 种；`regulatory_penalty`、`product_price_increase`、`supply_chain_disruption` 零样本，`inquiry_letter_pressure` 仅 2 个、`investor_question_pressure` 仅 4 个。
- **影响**：谓词 Schema 宣称支持 9 类事件，但 3 类完全没数据支撑；答辩时“我们覆盖了 9 类事件”会被立即戳穿。
- **建议**：补 3 类缺失事件各 5–10 条样本；或主动把文档口径改成“当前实现 6 类、其余 3 类已定义待扩展”，保持诚实。

### 1.4 合格规则过少且支持度低
- **现状**：13 条候选规则中只有 2 条 `qualified`，支持度仅 10–11 篇；`win_rate` 仅 0.55–0.60。
- **影响**：因子公式里“冻结规则评分和”几乎总是由同一两条规则贡献，因子区分度有限。
- **建议**：
  - 扩大 Discovery 样本后，规则搜索空间会更充分。
  - 审视 `evaluate_rule_candidate` 的合格门槛（`MIN_RULE_OCCURRENCES=5`、`MIN_RULE_WIN_RATE=0.50`、`MIN_RULE_AVG_EVIDENCE=0.75` 等）：是否因 `avg_evidence` 卡住大多数候选。
  - 增加“单谓词规则”搜索（当前从 2 阶组合起），以及为规则加事件类型约束，可能找到更多可解释、可支撑的规则。

---

## 2. 中等问题（一致性与展示）

### 2.1 模型配置在三个位置不一致（易被评委发现）
- **现状**：
  - `src/ai/gateway.py` 的 `AISettings.from_environment()` 默认值为 **OpenAI 口径**：`base_url=https://api.openai.com/v1`、`chat_model=gpt-5-mini`、`embedding_model=text-embedding-3-small`。
  - `/api/status` 返回的 `ai` 块因此显示 `gpt-5-mini` + `api.openai.com`。
  - 前端 `app.js` 的 `loadStatus()` **硬编码**显示“DeepSeek 待凭证”。
  - `/api/audit` 的 `model.chat_model` 又**硬编码**为 `deepseek-v4-flash`。
  - 文档与真实请求路径（`request_ai_layer`）才是 `deepseek-v4-flash` + `api.deepseek.com`。
- **影响**：同一页面三处对“模型”的描述彼此矛盾。对于以“审计透明”为核心卖点的作品，这是最容易在答辩现场被抓包的硬伤。
- **建议（P1，改动很小）**：
  - 把 `from_environment()` 的默认值统一改为 DeepSeek：`base_url=https://api.deepseek.com`、`chat_model=deepseek-v4-flash`、`embedding_model=`。
  - 前端 `loadStatus` 不再硬编码品牌名，改读 `data.ai.chat_model` / `data.ai.provider`。
  - 审计页的 `model.chat_model` 从 `AI_LAYER.status()` 读取，避免三处各自为政。

### 2.2 `repository_commit` 依赖 git，脱 git 后显示 unknown
- **现状**：`server.py:repository_commit()` 用 `git rev-parse --short HEAD`；当前目录是 git 仓库所以返回 `f4b65f6`。但打包提交/评分时若不含 `.git/`，审计页会显示 `unknown`。
- **建议**：改为“先查 `VERSION` 文件，再回退到 git”；或提交时把 `.git/` 保留，并在提交包自检清单里加一条“审计页代码版本不是 unknown”。

### 2.3 源文本被“项目关联：…”注释污染
- **现状**：`raw_documents.csv` 的 `content` 字段 130/130 全部以“项目关联：本条样本仅用于 AlphaLens 的政策事件与主营链条相关性研究……”结尾。代码到处用 `.split("项目关联：", 1)[0]` 剥离后才做证据回溯。
- **影响**：功能上自洽，但**源文本不是纯净原文**。评委若直接打开 `raw_documents.csv`，会看到每篇文本混入项目自己的声明，可能质疑“真实文本核验”的纯度。
- **建议**：把研究声明移到独立字段（如新增 `note` 列）或从 `content` 中移除，保持 `content` 为可核验原文；`split` 剥离逻辑随之删除。

### 2.4 证据文本被硬截断
- **现状**：`evidence_sentence()` 取 `sentence[:80]`，34 个事件的 `evidence_text` 长度正好 80，可能在句子中间被切断。
- **影响**：展示时看起来像半句话，影响“证据可回溯”的说服力。
- **建议**：改为按句子边界截断（`.rstrip` 到最近的 `。`/`；`），或允许稍长（120 字符）并同步放宽校验上限。

### 2.5 接口冗余：`/api/ai/status` 未被使用
- **现状**：`server.py` 提供 `/api/ai/status`，但前端只用 `/api/status` 内的 `ai` 块；两处数据源不同（前者 `AI_LAYER.status()`，后者同源）但未被消费。
- **建议**：删除该端点，或让前端统一走它，避免维护两份状态契约。

---

## 3. 轻微问题（工程健壮性 / 潜在风险）

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| 3.1 | `app/server.py` | `AI_LAYER` 为模块级单例，BGE `_MODEL` 为模块级全局；当前单 worker dev 服务器可运行，但多 worker 部署会有重复初始化 / 并发风险 | 引入进程级懒加载或加锁；若仅演示可加注释说明不支持多 worker |
| 3.2 | `server.py:research_audit` | `min(row["trade_date"] for row in market)` / `max(...)` 在 market 为空时抛 `ValueError` | 加空表保护（当前数据非空，属防御性缺口） |
| 3.3 | `server.py:historical_backtest` | `int(oos_metrics.get("rank_ic_valid_date_count", 0))` 依赖该字段为整数字符串；若上游改为 `"0.000000"` 格式会崩溃 | 统一走 `numeric_value()` 再做 `int()` |
| 3.4 | `live_analysis.py` | 每次分析都 `load_impact_priors()` 重读 CSV | 可缓存到模块级（量小，非必须） |
| 3.5 | `demo_engine.py` | `compute_forward_returns()` 用 `future_rows[4]`/`[9]` 取第 5/10 个交易日，要求每只股票行情足够密集；个别股票缺数据会整条跳过（当前 1 个事件被跳过） | 可加入“缺失容忍”或日志提示跳过原因 |
| 3.6 | `tests/` | 测试主要覆盖 happy path 与核心门控；未覆盖空数据、`/api/ai/check` 全错误码、前端渲染 | 补充边界测试；`项目历史资料/自动测试/` 有旧版 Playwright 配置可移植复用 |
| 3.7 | git 工作区 | `AlphaLens演示答辩.pptx` 处于“已删除未提交”状态（`git status` 显示 ` D`） | 确认答辩 PPT 是否仍需要；若需要请 `git checkout` 恢复 |

---

## 4. 数据质量具体改进清单

1. **补齐四格**：Discovery 公告 +23、Discovery 互动问答 +25、OOS 政策 +20、OOS 新闻 +12（以审计页实时数字为准）。
2. **补 3 类事件样本**：监管处罚 / 产品提价 / 供应链中断各 ≥5 篇，让 9 类事件都有据可查。
3. **生成历史 AI 标注缓存**：`DEEPSEEK_API_KEY=… .venv/bin/python 批量生成AI标注.py`，让审计页“历史 AI 缓存”从 0/130 变为可用的完整状态——答辩时这是“大模型参与”的实证。
4. **重算后重新抽查**：每次补数后用 `运行研究流水线.py` 安全重算，并用 `项目历史资料/研发脚本/validate_*.py` 系列做交叉校验。

---

## 5. 比赛包装与答辩建议

1. **主动披露 OOS 不足**：把“OOS 有效日期不足 → 证据不足 → 既不能证明有效也不能断言失效”写进 PPT“风险与局限”页，反而体现严谨。
2. **强调可复现性**：实测重算后 18 个 CSV 逐文件一致——这是很好的卖点，答辩可现场演示。
3. **补 Demo 录屏**：README 有三分钟演示步骤，但没有录屏文件；建议录一段 2–3 分钟（含断网冻结回放），放进提交包。
4. **统一口径**：所有材料（PPT、技术文档、答辩稿）统一说“DeepSeek V4 Flash + 19 谓词 + 三层门控 + Discovery/OOS”，与代码一致，避免 2.1 的不一致被放大。
5. **时间管理**：按 `项目历史资料/参考文档/赛题与工作说明.md` 的阶段表，8-15 前定稿成果包；本周集中补数据（1.2/1.3）优先级最高。

---

## 6. 待办清单（按优先级）

### P0（影响提交与评分，已工具化，按清单执行）
- [x] 运行 `检查数据覆盖.py` 查看最新待补清单（2026-08-06 实测：8 项缺口收敛到 1 项）
- [x] 按清单采集真实文本 → `导入真实文本.py 清单.csv`（暂存校验）→ `--apply` 入库（已补 66 篇：OOS 政策 20、OOS 新闻 12、discovery 公告 25、事件类型新闻 9）
- [x] 重算流水线 `运行研究流水线.py`，复查 `检查数据覆盖.py` 与 OOS 指标（OOS 有效 IC 截面 0→13）
- [ ] 补 discovery/互动问答 25 篇（唯一剩余缺口）：按 `data/external/文本导入暂存/互动问答采集模板.csv` 团队手动采集 → `导入真实文本.py --apply`

### P1（一致性 / 展示硬伤）
- [x] 2.1 统一 DeepSeek 模型配置（gateway 默认值 + 前端硬编码 + 审计页硬编码）
- [x] 2.2 `repository_commit` 增加 `VERSION` 文件兜底（新增 `VERSION` 文件）
- [ ] 2.3 把“项目关联：”注释从 `content` 拆出（**保留中，团队决策**；代码已正确处理，需团队手工确认后重写 CSV）
- [x] 2.4 证据文本按句子边界截断
- [ ] 生成历史 AI 标注缓存，让审计页数据完整（需 DeepSeek Key）——**团队任务**

### P2（打磨 / 健壮性）
- [x] 2.5 统一 `/api/ai/status` 与 `/api/status` 口径（保留端点，配置一致）
- [x] 3.1–3.5 防御性与并发加固（市场空表、指标转换、缺失日志；3.1 单例仅需在文档标注单 worker 限制）
- [x] 3.6 补充边界测试（新增 2 项；前端 Playwright 测试可移植旧版配置）
- [ ] 3.7 确认答辩 PPT 状态（`git status` 显示 `AlphaLens演示答辩.pptx` 已被删除未提交）
- [ ] 录制 Demo 录屏、更新提交包自检报告

---

**AlphaLens 是量化研究助手，不预测股票价格，不提供买卖建议。**
