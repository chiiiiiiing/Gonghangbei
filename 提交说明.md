# AlphaLens · 比赛提交说明

> 版本 `AlphaLens-Demo-v2.0-R1` ｜ 整理日期 2026-08-10
>
> **本报告仅供研究参考，不构成投资建议。**

## 可演示成果文件夹

上级目录的 `工行杯_AlphaLens/` 是本次按工行杯要求整理的精简提交目录，**不含 PPT 和研发过程材料**。其中包含：

```text
工行杯_AlphaLens/
├── 提交说明.md
├── README.md                         ← 产品、架构、截图、性能、商业化、视频链接
├── AlphaLens_v2.0-R1_提交包.zip     ← 解压即运行的代码、样例数据、文档、截图与录屏
├── 演示视频/AlphaLens_演示录屏.webm
├── 演示截图/                         ← 10 张核心模块截图（含新文本首页、研究验证模型/审计区和预测报告）
├── 完整说明文档.md
├── AI标注拒绝分析.md
└── 原创性证明.md
```

提交包以 `AlphaLens/` 为根目录；不包含 `.git`、`.venv`、`data/raw`、`data/processed`、`data/external`、API Key、`__pycache__` 或临时文件。

## 与赛事材料要求的对应

| 要求 | 交付位置 | 当前状态 |
|---|---|---|
| 可运行 Agent 应用：代码 + 录屏视频（README 放视频链接） | `AlphaLens_v2.0-R1_提交包.zip`；`演示视频/`；README 的 GitHub 视频链接 | 代码已就绪；v2 录屏待最终重录 |
| 项目源码 | GitHub `chiiiiiiing/Gonghangbei` 的 `codex/demo-integration` 分支 | 本轮自动提交并推送 |
| 技术文档：产品文档、架构图、核心模块截图、性能数据 | 根目录与包内 README、完整说明文档、演示截图 | 已就绪 |
| 商业化说明：场景、客户、模式 | 包内 README「商业化说明」 | 已就绪 |

## 一键启动

```bash
unzip AlphaLens_v2.0-R1_提交包.zip
cd AlphaLens
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
.venv/bin/python 启动演示.py
# 浏览器打开 http://127.0.0.1:8701
```

断网时可运行「冻结回放」完整演示；实时 AI 只接受当次请求中的真实 DeepSeek API Key，缺 Key、接口失败、模型身份不符或结构化结果不合法时会明确报错，不会伪装为 AI 成功。

## v2.0-R1 交付核查

- [x] 版本命名统一为 `AlphaLens-Demo-v2.0-R1`
- [x] 原 AI/19谓词/门控/冻结规则链路保持不变；新增“仅本次新文本”行业同比预测和报告
- [x] 冻结规则只以通过谓词门控的 `agreed_true` 条件匹配；冲突融合值不触发冻结规则
- [x] HTML/PDF 正文抓取后的全文可作为严格连续证据校验文本
- [x] 历史 AI 标注 33 条拒绝已按类别展示；重试仅针对失败文档且不放宽校验
- [x] Discovery 互动问答仅可通过官方候选暂存器进入人工复核；请求日期与列表/详情真实日期双重校验，联网验收未找到候选即如实保留 0/25 缺口
- [x] 10张核心截图（含桌面/移动新文本首页、研究验证的模型区与审计区、单文本同比预测报告）、录屏、产品文档、架构图、性能数据和商业化说明均进入可演示成果目录
- [x] 使用真实 DeepSeek Key 完成一次实时全链路验收：模型身份校验与政策链接全文分析均为 HTTP 200；5 只股票、完整 19 谓词 Schema，门控未通过时未触发冻结规则，Key 未保留
- [ ] 单篇文本同比预测全链路需在最终提交前重录并替换现有旧版演示

测试、编译、研究流水线和 ZIP 完整性结果记录在本次交付报告中；所有重算均只使用 `.venv/bin/python 运行研究流水线.py`，不会覆盖人工核验的 `raw_documents.csv`。
