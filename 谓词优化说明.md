# 谓词优化规格说明

---

## 1. 优化背景与目标

基于历史数据诊断，19 个谓词存在以下问题：

1. **5 个谓词近乎恒值**：同一答案占比超过 97%，无区分度，白白增加 AI 抽取与校验成本。
2. **规则主力谓词区分度不足**：现有规则引用次数最多的谓词，IC 接近 0 或为负，导致因子样本外（OOS）表现不佳。
3. **谓词数据陈旧**：当前交付的 `predicates.csv` 与当前事件数据不一致，需重新生成。

**优化目标**：
- 19 个谓词均具备非平凡区分度，无恒值谓词；
- 判定逻辑在批量模式与实时模式保持一致；
- 为规则方提供每个谓词的信号方向（IC），供其组合规则。

---

## 2. 数据口径

- **诊断样本**：以当前代码对 349 个事件重新落地谓词，其中 343 个事件有可用的 5 日收益。
- **IC（信息系数）**：谓词取值（布尔为 0/1，数值为原始值）与 5 日收益的相关系数。IC 为正表示"该谓词为真时收益倾向更高"，为负表示"为真时收益倾向更低"，接近 0 表示无预测力。
- **说明**：本文档内 IC 为未中性化的原始收益相关；官方回测指标为行业中性口径（OOS Rank IC = -0.026），两者结论一致。

---

## 3. 修改清单

### 3.1 全部 19 个谓词的处理决定

| 谓词 | 处理 | 决定说明 |
|---|---|---|
| `has_policy_support` | **重定义** | 现定义与事件类型冗余（IC -0.007），改为内容驱动判定 |
| `policy_directly_related_to_business` | 保留 | 判定随 has_policy_support 重定义自动更新 |
| `event_mentions_core_product` | **保留** | 唯一弱正信号（IC +0.032） |
| `evidence_from_authoritative_source` | 保留 | 方向为负（IC -0.074），由规则方按负信号处理 |
| `source_government_or_exchange` | **删除并替换** | 与 evidence 同值率 74%，冗余无增量信息 |
| `source_company_announcement` | 保留 | IC -0.006，规则方不建议依赖 |
| `source_major_media` | 保留 | 方向为负（IC -0.066），由规则方按负信号处理 |
| `social_attention_spikes` | **删除并替换** | 死题（6/343），IC≈0 |
| `policy_attention_followup` | **收紧判定** | 真值率 51%（175/343）过高，收窄触发关键词 |
| `institutional_attention_increases` | **删除并替换** | 死题（2/343） |
| `investor_questions_increase` | **修复判定** | 触发率过低（4/343），扩展触发条件 |
| `management_response_vague` | **删除并替换** | 死题（0/343） |
| `announcement_contains_uncertainty` | **修复判定** | 触发率过低（4/343），扩展触发条件 |
| `risk_or_uncertainty_disclosure` | 保留 | 正信号（IC +0.052） |
| `demand_side_policy` | 保留 | 方向为负（IC -0.097），由规则方按负信号处理 |
| `supply_side_policy` | 保留 | 弱正（IC +0.022），触发率低，建议规则方评估 |
| `capacity_policy_support` | 保留 | 方向为负（IC -0.074），由规则方按负信号处理 |
| `event_evidence_strength` | 保留 | 数值型，作倍率 |
| `event_has_short_term_price_impact` | 保留 | 数值型，作倍率 |

**汇总**：删除并替换 4 个、修复判定 2 个、重定义 1 个、收紧判定 1 个、保留 11 个。总量保持 19 个。

**删除并替换对照（4 个）**：

| 删除的旧谓词 | 删除原因 | 替换为新谓词 |
|---|---|---|
| `management_response_vague` | 死题（0/343），无信息 | `event_has_quantitative_target` |
| `institutional_attention_increases` | 死题（2/343），无信息 | `event_mentions_export` |
| `social_attention_spikes` | 死题（6/343），IC≈0 | `event_scale_industry_level` |
| `source_government_or_exchange` | 与 evidence 同值率 74%，冗余 | `event_policy_binding_strength` |

**替换理由（为什么换成新谓词）**：

共同逻辑：删除的都是恒值或来源/聚合驱动的谓词（IC≈0 或与现有题重复）；诊断显示区分度较高的信号集中在事件具体内容维度，而来源类、聚合类谓词的 IC 普遍接近 0。新谓词均为内容驱动，填补当前 19 题未覆盖的维度。

- `management_response_vague → event_has_quantitative_target`：旧题 0/343 从未触发、零信息。新题衡量"事件是否落地到具体数字/金额"——量化内容比定性表态更有区分度，可区分"有明确金额目标"与"空泛支持"两类事件。
- `institutional_attention_increases → event_mentions_export`：旧题依赖机构调研/研报的时间窗聚合，语料几乎不出现（2/343）且实时模式无法计算。新题衡量"事件是否涉出口/海外市场"——出口信号是稳定正信号（IC5 +0.069、IC10 +0.140），且为当前 19 题未覆盖的内容维度。（原候选 `event_mentions_execution_schedule` 实测两个周期 IC≈0，已淘汰）
- `social_attention_spikes → event_scale_industry_level`：旧题依赖时间窗聚合、IC≈0。新题衡量"事件作用范围是产业级还是公司级"——诊断显示事件颗粒度影响收益（产业级事件偏 beta、公司级事件偏 alpha），当前 19 题未覆盖。
- `source_government_or_exchange → event_policy_binding_strength`：旧题与 evidence 同值率 74%、冗余无增量。新题衡量"政策约束强度（强制/必须 vs 鼓励/支持）"——强约束与弱约束政策的市场影响不同，是当前 19 题缺失的内容维度。

### 3.2 新谓词实现规格（替换 4 个，均为布尔型）

| 新谓词 | 中文定义 | 确定性判定逻辑（关键词/正则，实现时需用样本微调） |
|---|---|---|
| `event_has_quantitative_target` | 事件文本是否包含可量化目标或金额 | 文本含"数字+单位"（金额/百分比/GWh/辆/台/万吨等），或含"目标/力争/不低于/达到"且同句含数字 |
| `event_mentions_export` | 事件是否涉及出口/海外市场 | 文本含"出口/海外/国际市场/外销/出海/出口量/外贸" |
| `event_scale_industry_level` | 事件作用范围是否为产业级/行业级 | 文本含"行业/产业/产业链/全国/多家企业/龙头"，且非单一公司公告 |
| `event_policy_binding_strength` | 政策是否含强约束表述（强制/必须/不得） | 文本含"强制/必须/不得/严禁/限期/责令"等强约束词（区别于"鼓励/支持/引导"） |

**4 个新谓词离线验证结果（343 事件实测）**：

| 新谓词 | 触发率 | IC(5日) | IC(10日) | 验证结论 |
|---|---|---|---|---|
| `event_has_quantitative_target` | 157/343 | +0.048 | +0.147 | ✅ 通过 |
| `event_mentions_export` | 40/343 | +0.069 | +0.140 | ✅ 通过 |
| `event_scale_industry_level` | 150/343 | +0.038 | +0.039 | ✅ 通过 |
| `event_policy_binding_strength` | 26/343 | +0.054 | +0.122 | ✅ 通过 |

> 原候选 `event_mentions_execution_schedule` 实测两个周期 IC≈0（收紧判定后仍为 -0.044/-0.029），无区分度，已淘汰并替换为 `event_mentions_export`。另：`has_policy_support` 重定义后 IC 从 -0.007 升至 +0.049，验证有益。

### 3.3 判定逻辑修改规格（4 个）

> 均在 `src/pipeline/ground_predicates_rule_based.py` 的 `ground_event_predicates()` 内修改；谓词定义变化后 AI 侧描述（`src/ai/prompts.py`）需同步。

| 谓词 | 动作 | 为什么改（现状问题） | 怎么改（代码位置 + 改动） | 影响面 | 效果 |
|---|---|---|---|---|---|
| `has_policy_support` | 重定义 | 现判定=事件类型==policy_support，与事件类型冗余，IC -0.007 | 第115行：has_policy 改为「来源为policy或含政策文件词（印发/通知/意见/方案/支持）」且「含政策载体词（补贴/规划/专项行动/实施意见/指导意见/工作方案/扶持）」 | 连带 5 个：policy_directly_related / demand / supply / capacity / policy_followup | 真值率 42%→下降，不再冗余 |
| `policy_attention_followup` | 收紧 | 11 个关键词任一命中即触发，含"补贴/市场关注/多家"过宽，真值率51%，IC -0.020 | 第124行：改为「含跟进动词（后续/进一步/部署/跟进）」或「含强信号词（行动方案/试点/以旧换新/设备更新/招标规模/装机量/装车量/渗透率）且含量化表述」 | 仅自身 | 真值率 51%→44%，IC 从 -0.020 降至 -0.071（成为更清晰的负信号，规则方宜按反向使用） |
| `investor_questions_increase` | 修复 | 判定=事件类型==investor_question_pressure，只覆盖单一类型，触发率 4/343，IC 最强 -0.137 | 第140行：改为「来源为 ir_qa 或 正文含"投资者提问/互动易/上证e互动/追问"」 | 仅自身 | 触发率上升，负信号获得样本 |
| `announcement_contains_uncertainty` | 修复 | 判定=公告来源且含不确定性词，非公告漏判，触发率 4/343，IC -0.082 | 第142行：去掉「公告来源」前置条件，任意来源正文含"不确定/风险/可能影响/提示" | 连带 risk_or_uncertainty_disclosure | 触发率上升，负信号获得样本 |

---

## 4. 修改后的最终谓词（19 个）

本节为优化后完整的 19 个谓词清单，即最终 Schema，规则方与实现方以此为准。

| 序号 | 谓词 | 定义（修改后） | 取值 | 本次处理 |
|---|---|---|---|---|
| 1 | `has_policy_support` | 文本含政策支持载体（补贴/规划/专项行动/实施意见/指导意见/工作方案/扶持），且来源为 policy 或含政策文件特征词 | 布尔 | 重定义 |
| 2 | `policy_directly_related_to_business` | 政策支持且直接作用于公司主营业务或产业链核心环节 | 布尔 | 保留（判定随 1 自动更新） |
| 3 | `event_mentions_core_product` | 原文提及公司的核心产品或关键业务 | 布尔 | 保留 |
| 4 | `evidence_from_authoritative_source` | 证据来自政府、交易所、公司公告或权威财经媒体 | 布尔 | 保留 |
| 5 | `event_policy_binding_strength` | 政策含强约束表述（强制/必须/不得/严禁/限期/责令） | 布尔 | 新增 |
| 6 | `source_company_announcement` | 来源是上市公司正式公告 | 布尔 | 保留 |
| 7 | `source_major_media` | 来源是主流财经或综合媒体 | 布尔 | 保留 |
| 8 | `event_scale_industry_level` | 事件作用范围是否为产业级/行业级 | 布尔 | 新增 |
| 9 | `policy_attention_followup` | 政策发布后存在后续部署、试点或跟进信号 | 布尔 | 收紧判定 |
| 10 | `event_mentions_export` | 事件是否涉及出口/海外市场 | 布尔 | 新增 |
| 11 | `investor_questions_increase` | 文本体现投资者提问或集中追问增加 | 布尔 | 修复判定 |
| 12 | `event_has_quantitative_target` | 事件文本是否包含可量化目标或金额 | 布尔 | 新增 |
| 13 | `announcement_contains_uncertainty` | 公告包含审批、交付、价格或履约不确定性 | 布尔 | 修复判定 |
| 14 | `risk_or_uncertainty_disclosure` | 文本明确披露风险或不确定性 | 布尔 | 保留 |
| 15 | `demand_side_policy` | 政策主要作用于消费、采购或终端需求 | 布尔 | 保留 |
| 16 | `supply_side_policy` | 政策主要作用于供给、技术、产能或产业建设 | 布尔 | 保留 |
| 17 | `capacity_policy_support` | 政策明确支持产能、项目或基础设施建设 | 布尔 | 保留 |
| 18 | `event_evidence_strength` | 事件证据强度（0-1） | 数值 | 保留 |
| 19 | `event_has_short_term_price_impact` | 类似事件潜在短期影响强度（0-1） | 数值 | 保留 |

**改动汇总**：新增 4 个（event_policy_binding_strength / event_scale_industry_level / event_mentions_export / event_has_quantitative_target）；重定义 1 个（has_policy_support）；收紧 1 个（policy_attention_followup）；修复 2 个（investor_questions_increase / announcement_contains_uncertainty）；其余保留。总量仍为 19 个。

---

## 5. IC 信号报告

### 5.1 重建后实测（当前代码，349 事件 / 343 有收益）

| 谓词 | IC(5日) | IC(10日) | 方向 | 建议用法 |
|---|---|---|---|---|
| `investor_questions_increase` | -0.137 | -0.011 | 负 | 反向条件/负信号（触发率低，罕见但强） |
| `capacity_policy_support` | -0.100 | -0.084 | 负 | 反向条件（现有规则按正信号使用，需复核） |
| `demand_side_policy` | -0.093 | -0.080 | 负 | 反向条件（R007 方向待复核） |
| `policy_directly_related_to_business` | +0.083 | +0.127 | 正 | 正信号 |
| `evidence_from_authoritative_source` | -0.074 | -0.158 | 负 | 规则引用最多但方向为负，勿当正信号 |
| `policy_attention_followup` | -0.071 | -0.030 | 负 | 收紧后为更清晰负信号 |
| `event_mentions_export` | +0.069 | +0.140 | 正 | 新增，正信号 |
| `source_major_media` | -0.066 | -0.193 | 负 | 反向条件 |
| `event_policy_binding_strength` | +0.057 | +0.137 | 正 | 新增，正信号 |
| `has_policy_support` | +0.049 | +0.053 | 正 | 重定义后由负转正 |
| `event_has_quantitative_target` | +0.048 | +0.147 | 正 | 新增，正信号 |
| `event_has_short_term_price_impact` | +0.044 | +0.098 | 弱正 | 数值型，作倍率 |
| `event_evidence_strength` | +0.041 | +0.056 | 弱正 | 数值型，作倍率 |
| `event_scale_industry_level` | +0.038 | +0.039 | 弱正 | 新增，弱正 |
| `risk_or_uncertainty_disclosure` | +0.035 | +0.084 | 正 | 正信号 |
| `event_mentions_core_product` | +0.032 | +0.061 | 弱正 | 可组合 |
| `announcement_contains_uncertainty` | +0.017 | +0.131 | 弱正 | 修复后触发率上升 |
| `supply_side_policy` | +0.009 | -0.023 | 无 | 建议不依赖 |
| `source_company_announcement` | -0.006 | -0.028 | 无 | 建议不依赖 |

### 5.2 优化前基线（供对比，343 事件）

| 谓词 | IC | 方向 | 建议用法 |
|---|---|---|---|
| `investor_questions_increase` | -0.137 | 负 | 反向条件 / 负信号 |
| `demand_side_policy` | -0.097 | 负 | 反向条件（现有 R007 按正信号使用，方向待复核） |
| `announcement_contains_uncertainty` | -0.082 | 负 | 反向条件 / 负信号 |
| `capacity_policy_support` | -0.074 | 负 | 反向条件（现有规则按正信号使用，方向待复核） |
| `evidence_from_authoritative_source` | -0.074 | 负 | 现有规则引用 8 次且按正信号使用，方向待复核 |
| `source_major_media` | -0.066 | 负 | 反向条件 |
| `risk_or_uncertainty_disclosure` | +0.052 | 正 | 正信号 |
| `event_has_short_term_price_impact` | +0.044 | 弱正 | 数值型，作倍率 |
| `event_evidence_strength` | +0.041 | 弱正 | 数值型，作倍率 |
| `source_government_or_exchange` | -0.037 | 负 | 与 evidence 重叠，建议不单独使用 |
| `event_mentions_core_product` | +0.032 | 弱正 | 可与 policy_followup 组合 |
| `institutional_attention_increases` | +0.028 | 弱正 | 将替换 |
| `supply_side_policy` | +0.022 | 弱正 | 触发率低，建议评估 |
| `policy_attention_followup` | -0.020 | 负 | 现有规则引用 4 次，方向待复核 |
| `has_policy_support` | -0.007 | 无 | 建议不依赖 |
| `source_company_announcement` | -0.006 | 无 | 建议不依赖 |
| `social_attention_spikes` | -0.006 | 无 | 将替换 |
| `policy_directly_related_to_business` | +0.002 | 无 | 建议不依赖 |
| `management_response_vague` | 0.000 | 无 | 将替换 |

**规则方注意事项**：
1. 现有规则将 `evidence_from_authoritative_source` 作为正信号（引用 8 次），但其 IC 为负，方向可能相反；
2. R007（`has_policy_support AND demand_side_policy`）当前按正信号使用，而 demand_side_policy 的 IC 为负；
3. 建议规则层支持 "NOT 条件 / 负信号" 表达，以利用负向谓词；
4. 现有 qualified 规则（R001–R005）的 OOS 收益均为负，建议以 OOS 指标为准重新评估。

---

## 6. 实施与验证

### 涉及文件

| 文件 | 改动 |
|---|---|
| `src/ai/prompts.py` | `PREDICATE_DEFINITIONS`：更新 19 个谓词定义 |
| `src/pipeline/ground_predicates_rule_based.py` | `PREDICATES` 列表 + `ground_event_predicates()`：新增/修改判定逻辑 |
| `src/ai/research_layer.py` | `BOOLEAN_PREDICATES` 自动派生自 `PREDICATE_DEFINITIONS`，无需手改 |
| `tests/test_alphalens.py` | 同步谓词名引用 |

### 重建与验收

1. 运行 `python3 运行研究流水线.py`，重建 `predicates.csv` / `rules.csv` / `factors.csv`（SHA 校验仅针对 `raw_documents.csv`，正常通过）；
2. 运行 `python3 -m pytest tests/`，全部测试通过；
3. 重新计算各谓词 IC：新谓词 IC 非 0、修复谓词触发率上升、无恒值谓词；
4. 验收线：因子 OOS Rank IC 由 -0.026 转正。

### 协同说明

`predicates.csv` 与 `rules.csv` 由同一条流水线重建，建议谓词方与规则方约定同一时间运行，避免一方覆盖另一方的工作。
