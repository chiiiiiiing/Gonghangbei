# AlphaLens 服务器部署指南

> 适用版本：AlphaLens-Demo-v2.0-R1 及之后
> 最后更新：2026-08-13

本指南描述如何把 AlphaLens 部署到个人服务器并通过 HTTPS 访问。

线上地址：**https://hechenyu.xin/s/GongHangBei/**

---

## 一、服务器信息

| 项 | 值 |
|---|---|
| SSH | `root@114.55.135.102` |
| 域名 | hechenyu.xin |
| 部署目录 | `/var/www/personal-site/s/GongHangBei/` |
| 服务端口 | 8701（仅监听 127.0.0.1，由 nginx 反向代理对外） |
| systemd 服务 | `alphalens.service` |
| nginx 配置 | `/etc/nginx/sites-enabled/personal-site` |
| 公网 URL | `https://hechenyu.xin/s/GongHangBei/` |

---

## 二、首次部署（从零开始）

### 1. 准备服务器目录

```bash
ssh root@114.55.135.102 "mkdir -p /var/www/personal-site/s/GongHangBei"
```

### 2. 安装 Python 依赖

服务器为 Ubuntu（Python 3.10），按锁定依赖文件安装：

```bash
ssh root@114.55.135.102 "pip3 install --ignore-installed blinker -r /var/www/personal-site/s/GongHangBei/requirements.txt"
```

> 说明：`--ignore-installed blinker` 是因为系统自带的 distutils 版 blinker 会阻止 Flask 正常安装。

### 3. 上传项目

正式上线应从目标 Git 提交生成发布包，避免把未提交文件、开发缓存和本地凭证混入服务器。以下示例部署当前 `HEAD`：

```bash
COMMIT=$(git rev-parse HEAD)
git archive --format=tar.gz --output=/tmp/alphalens-${COMMIT}.tar.gz "$COMMIT"
scp /tmp/alphalens-${COMMIT}.tar.gz root@114.55.135.102:/tmp/
```

服务器先解压到临时发布目录，验收后再切换到正式目录。`data/sample/` 必须完整包含在 Git 提交中，否则服务会报数据缺失。

`.env` 永远不进入 Git 发布包，也不能用普通 `rsync .` 上传。需要服务器默认启用实时 LLM 时，单独传输并限制权限：

```bash
scp .env root@114.55.135.102:/tmp/alphalens.env
ssh root@114.55.135.102 "install -m 600 /tmp/alphalens.env /var/www/personal-site/s/GongHangBei/.env && rm -f /tmp/alphalens.env"
```

### 4. 配置 systemd 服务

```bash
ssh root@114.55.135.102 'cat > /etc/systemd/system/alphalens.service << "EOF"
[Unit]
Description=AlphaLens Flask API
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/var/www/personal-site/s/GongHangBei
EnvironmentFile=-/var/www/personal-site/s/GongHangBei/.env
ExecStart=/usr/bin/python3 启动演示.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF
systemctl daemon-reload
systemctl enable alphalens
systemctl restart alphalens'
```

> 旧版 ExecStart 为 `app/server.py`。新版统一入口是 `启动演示.py`（内部 runpy 调用 `app/server.py` 并设置工作目录），两者均可，但建议统一使用 `启动演示.py`。

### 5. 配置 nginx 反向代理

在 `/etc/nginx/sites-enabled/personal-site` 的 `location /s/` 块内、`location /s/lexsmart/` 之后插入：

```nginx
    # AlphaLens Financial Factor Mining
    location /s/GongHangBei/ {
        proxy_pass http://127.0.0.1:8701/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
```

> 注意：该配置在两个 server 块（80 和 443）中各需要一份。用 `nginx -T | grep -c 'location /s/GongHangBei'` 验证应为 2。

校验并重载：

```bash
ssh root@114.55.135.102 "nginx -t && nginx -s reload"
```

### 6. 验证部署

```bash
# 服务状态
ssh root@114.55.135.102 "systemctl status alphalens --no-pager | head -6"

# 页面
curl -sI https://hechenyu.xin/s/GongHangBei/ | head -3

# API
curl -s https://hechenyu.xin/s/GongHangBei/api/status | head -c 300
```

---

## 三、日常更新（增量部署）

本地改完代码并提交后，仍使用“Git archive → 临时目录 → 原子切换”的方式。不要用工作区全量 `rsync` 覆盖线上目录；这样可以保证服务器版本与 PR head 一致，并保留可回滚目录。

```bash
# 发布包切换完成后重启服务
ssh root@114.55.135.102 "systemctl restart alphalens"

# 确认状态
ssh root@114.55.135.102 "systemctl status alphalens --no-pager | head -4"
```

> 前端资源（`app/assets/`、`app/vendor/`）更新后无需重启服务，刷新浏览器即可；后端（`app/server.py`、`src/`、`data/`）变更必须重启。

---

## 四、常见问题

### Q1：`Address already in use` / 端口被占用

旧进程未释放 8701 端口：

```bash
ssh root@114.55.135.102 "fuser -k 8701/tcp; systemctl restart alphalens"
```

### Q2：`ModuleNotFoundError: No module named 'flask'`

pip 装到了别的 Python。确认：

```bash
ssh root@114.55.135.102 "python3 -c 'import flask; print(flask.__version__)'"
```

失败则按"二、2"重装。

### Q3：前端 fetch 报 `Unexpected token '<'`

API 请求返回了 HTML 而非 JSON，通常是路径问题：

- 前端必须用**相对路径** `fetch('api/analyze')`，不能写 `/api/analyze`（绝对路径会绕过 nginx 的 `/s/GongHangBei/` 前缀）。
- 检查 nginx 两个 server 块都有 `location /s/GongHangBei/` 代理。

### Q4：CDN 资源被 CSP 拦截

服务器 CSP 只允许 `cdnjs.cloudflare.com`、`unpkg.com` 等白名单域名。前端外链资源（Plotly 等）必须使用这些域名，或放入 `app/vendor/` 本地提供（当前做法，无外网依赖）。

### Q5：测试中 PDF 抓取用例失败

`test_fetch_full_text_pdf_extracts_text` 依赖外网抓取 cninfo PDF，纯网络环境问题，与代码无关。本地跑全量测试前挂代理即可。

### Q6：服务器上代码版本混乱

部署目录曾混入旧版文件（`run_pipeline.py`、`查看材料/` 等）。以 `VERSION` 文件和 `启动演示.py` 是否存在判断版本：

```bash
ssh root@114.55.135.102 "cat /var/www/personal-site/s/GongHangBei/VERSION"
```

若输出不是预期版本号，重新执行"二、3"全量 rsync。

---

## 五、服务器备忘

| 操作 | 命令 |
|---|---|
| 查看日志 | `ssh root@114.55.135.102 "journalctl -u alphalens -n 50 --no-pager"` |
| 重启服务 | `ssh root@114.55.135.102 "systemctl restart alphalens"` |
| 停止服务 | `ssh root@114.55.135.102 "systemctl stop alphalens"` |
| nginx 重载 | `ssh root@114.55.135.102 "nginx -s reload"` |
| 验证 nginx 代理 | `ssh root@114.55.135.102 "nginx -T \| grep -c 'location /s/GongHangBei'"`（应为 2） |

本报告仅供研究参考，不构成投资建议。
