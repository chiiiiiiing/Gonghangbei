# AlphaLens 历史 AI 标注拒绝分析（R4.1）

> 审计样本：`data/sample/ai_annotations.jsonl`，196 篇文档；163 篇严格校验成功、33 篇严格拒绝。
>
> **本报告仅供研究参考，不构成投资建议。**

## 拒绝分类

| 严格拒绝类别 | 数量 | 说明 | R4.1 处理 |
|---|---:|---|---|
| 事件或关系证据不是原文连续片段 | 23 | 模型给出的证据无法在输入原文逐字定位 | 仍要求连续子串；修复请求附带从原文截取的候选片段，禁止概括、拼接和改写 |
| 事件类型与来源类型不相容 | 6 | 例如公告被标为 `attention_spread`，新闻被标为 `policy_support` | Prompt 明确传入该来源允许的枚举；研究层仍做来源×事件类型硬校验 |
| 逐股票关系证据或股票池校验未通过 | 2 | 没有通过股票池及原文关系证据双重校验的逐股票结果 | 不补造公司关联；保留拒绝并在重试时重申逐股票证据契约 |
| 逐股票 19 个谓词不完整 | 1 | 返回缺失 `stock_analyses` 或每股票 19 谓词集合 | Prompt 与二次修复均要求每只股票恰好 19 个不重复英文谓词 |
| 模型未返回可解析的结构化 JSON | 1 | 两次请求均无法解析为 JSON object | 网关明确报错；不将规则程序结果伪装为 AI 返回 |

## R4.1 的严格修复原则

1. **没有放宽任何金融语义或证据校验**：事件类型仍受枚举与来源类型限制；股票代码仍必须来自锁定股票池；事件和关系证据仍必须是输入标题或有效正文中的连续原文子串。
2. **链接全文与验证器对齐**：抓取 HTML/PDF 全文成功时，模型与连续证据校验器使用同一份全文；不再出现模型正确引用全文、验证器却只在摘要中查找的范围不一致。
3. **修复请求可执行**：二次请求提供原文中截取的字面候选片段和来源限定事件类型，帮助模型纠正格式或引用，不接受模型“解释为什么它应该通过”。
4. **失败不计入成功**：审计按每篇文档的最新记录统计，失败项不会进入 RAG 成功索引或提升规则支持度。

## 真实 Key 验收后的重试

先在终端安全设置临时环境变量（不要写入 README、CSV、日志或 Git），再运行：

```bash
DEEPSEEK_API_KEY=你的Key .venv/bin/python 批量生成AI标注.py --retry-failed
```

该命令保留同一原文哈希的既有成功记录，只重试失败文档；缓存是 append-only 的，审计与脚本结论均以每篇文档最新记录为准。重试后必须运行完整测试和 `运行研究流水线.py`，并在研究审计页复核成功数、失败分类及免责声明。
