# AlphaLens 用户参与工作推进手册

生成日期：2026-07-15

本报告仅供研究参考，不构成投资建议

## 当前你要推进的事

当前自动线已经把文本、行情、事件、谓词、规则、因子、回测审计和报告串通。你接下来不需要从头重建项目，重点是给“自动候选版”补上人工确认记录，让它可以被 A、C 和最终 PPT 安全使用。

你已经完成或授权完成三项关键工作：真实文本来源核验、行情占位口径决定、事件人工抽检。接下来只推进 4 组工作：

1. 人工抽检谓词判断结果。
2. 与 A 确认金融 Schema 和对外表述。
3. 与 C 联调真实数据。
4. 做 PPT、报告与最终 CSV 数字交叉检查。

已完成记录：

- 真实文本：120 条程序化联网核验通过，policy / announcement / news / ir_qa 各 30 条，URL 全局唯一。
- 行情决定：接受 `adj_factor=1` 作为字段占位；它不是真实复权因子序列，必须在答辩中说明限制。
- 事件抽检：10 条均为 `drop`，已按共性误判根因修正抽取规则，原始结论继续保留作审计记录。

不要做的事：

- 不要运行 `--force-sample-generation`。
- 不要改 `参考文档/数据格式规范.md` 锁定的 CSV 字段名。
- 不要把系统描述成预测股价或给投资建议。
- 不要删除任何报告中的免责声明。
- 不要把 `data/raw/`、`data/processed/`、`data/external/` 放入 Git。

## 推荐推进顺序

### 第 1 步：真实文本来源核验（已完成）

本步不再需要你逐条操作。已完成以下检查：

1. 120 条文本全部联网刷新，四类来源各 30 条。
2. 政策只接受政府、部委和能源主管部门页面。
3. 公告使用巨潮资讯网 PDF；优先选择项目、订单、处罚、问询、经营异常或年度报告等权威证据文件。
4. 新闻只接受白名单财经媒体、行业协会、行业媒体或官方统计页面。
5. 互动问答使用深交所互动易详情页，保留真实问题和公司回复摘要。
6. 检查日期窗口、详情页形式、域名白名单、摘要结构、联网状态和 URL 唯一性。
7. 最终结果为 120/120 `pass`，全局 120 个唯一 URL。

查看证据：

- `查看材料/真实文本来源核验报告.md`
- `查看材料/源文本核验明细.csv`
- `查看材料/真实文本来源获取记录.md`

限制：这是程序化来源和语义规则核验，不等同于版权授权、交易所法律意见或数据供应商认证。

### 第 2 步：确认真实前复权行情口径（已完成决定）

决定：保留东方财富 `fqt=1` 前复权价格候选版，并接受 `adj_factor=1` 作为字段占位。

使用文件：

- `data/sample/market_data.csv`
- `查看材料/真实行情获取记录.md`
- `查看材料/真实行情校验报告.md`
- `查看材料/真实行情导入模板.csv`

当前状态：

- 行情来源：东方财富历史 K 线接口。
- 参数：日频 `klt=101`，前复权 `fqt=1`。
- 覆盖：30 只股票，18,002 行。
- 日期范围：2024-01-02 至 2026-06-30。
- 校验 errors：0。
- 已知 warning：个别股票上市时间较晚或停牌，覆盖行数少于最长序列；`adj_factor` 唯一值为 1。
- `adj_factor=1` 只用于满足锁定字段契约，不代表真实复权因子，也不能用于还原未复权价格。
- 当前回测只用于验证文本到因子再到回测的研究链路，不能作为投资结论。

答辩建议原话：

> 当前价格序列来自东方财富 `fqt=1` 前复权接口。接口没有单独返回复权因子，因此 `adj_factor=1` 仅为字段占位；正式接入金融数据供应商后可整体替换价格和复权因子并重新回测。

### 第 3 步：人工抽检事件结果（已完成并处理）

你完成的 10 条事件抽检均为 `drop`。这不是白做，反而准确暴露了两个共性缺陷：

使用文件：

- `查看材料/事件人工抽检样本.csv`
- `data/sample/events.csv`
- `data/sample/raw_documents.csv`
- `查看材料/人工抽检结果校验报告.md`

1. “项目关联”是系统补写说明，不能反过来证明原文存在产能扩张。
2. 一条互动问答不能证明“投资者提问压力增加”。

已实施处理：

- 事件和谓词只读取“项目关联：”之前的原文摘要。
- 公告只有出现明确事件事实才生成事件。
- 单条互动问答保留为证据文本，但不生成提问压力事件。
- 谓词不再通过系统生成的 `event.object` 自动命中核心产品。

查看闭环：`查看材料/事件抽检问题处理记录.md`。本轮事件抽检任务已完成，暂不需要重复填写。

### 第 4 步：人工抽检谓词结果

目标：确认每个事件被转成谓词时，true/false/分数是否符合金融语义。

使用文件：

- `查看材料/谓词人工抽检样本.csv`
- `data/sample/predicates.csv`
- `参考文档/事件与谓词Schema.md`
- `查看材料/A口径确认建议稿.md`

当前状态：

- 谓词总数：1496。
- 抽检样本：60 条。
- `manual_review_result` 目前为空。

操作步骤：

1. 打开 `查看材料/谓词人工抽检样本.csv`。
2. 按 `event_id` 分组看同一事件下多个谓词。
3. 查看 `predicate_name`、`value`、`confidence`、`rationale`。
4. 对照 `参考文档/事件与谓词Schema.md` 判断口径。
5. 在 `manual_review_result` 填 `pass`、`revise` 或 `drop`。
6. 在 `manual_comment` 写具体理由。

谓词重点检查：

| 谓词 | 重点判断 |
|------|----------|
| has_policy_support | 只有政策支持或补贴、减税、消纳、行动方案等才应为 true |
| policy_directly_related_to_business | 是否直接影响公司主营或核心产业链 |
| event_mentions_core_product | 是否明确涉及产品、技术、产能、订单或核心业务 |
| evidence_from_authoritative_source | 政府、交易所、巨潮、互动易等通常为 true，普通媒体需谨慎 |
| social_attention_spikes | 是否有关注度扩散证据，不能只因有一篇新闻就为 true |
| investor_questions_increase | 互动问答压力或集中提问才为 true |
| management_response_vague | 回复是否实质含糊、回避或缺少量化信息 |
| announcement_contains_uncertainty | 公告是否有不确定、风险、尚需审批、可能变化等表述 |
| event_evidence_strength | 0 到 1 分数，必须与来源权威性和证据直接性一致 |
| event_has_short_term_price_impact | 中性理解为历史关注强度，不要写成预测股价 |

合法值：

- 布尔谓词只能填 `true` 或 `false`。
- 分数谓词必须是 0 到 1 的数字字符串。
- 人工结论只能填 `pass`、`revise`、`drop` 或暂时留空。

完成后让 Codex 运行：

```bash
.venv/bin/python scripts/validate_manual_review_results.py
.venv/bin/python run_pipeline.py --preserve-inputs
```

### 第 5 步：找 A 确认金融 Schema

目标：让项目负责人确认事件类型、谓词和对外表述边界，避免 B/C 自动线在金融逻辑上跑偏。

使用文件：

- `查看材料/A口径确认建议稿.md`
- `参考文档/事件与谓词Schema.md`
- `查看材料/答辩问答素材.md`

你需要让 A 明确回答：

1. `policy_support` 是否包括产业行动方案、税收优惠、补贴、目录管理、消纳政策。
2. `capacity_expansion` 是否只接受明确的募投项目、产能建设和项目投产事实；评级报告中的背景描述不应自动算事件。
3. `investor_question_pressure` 需要多长时间窗和多少条提问才能成立；单条问答不能直接成立。
4. `social_attention_spikes` 的证据门槛是什么。
5. `event_has_short_term_price_impact` 是否需要改成更中性的名称。
6. 答辩中是否接受“另类因子研究信号”这个说法。
7. 是否明确禁止任何“预测股价、推荐买卖、保证收益”的措辞。

建议记录格式：

```text
A 口径确认记录：
日期：2026-xx-xx
参与人：
1. policy_support：采用/调整为……
2. capacity_expansion：采用/调整为……
3. investor_question_pressure：采用/调整为……
4. 对外表述：允许……，禁止……
需 B 修改：
需 C 修改：
```

确认后，把记录交给 Codex。Codex 可以继续做：

- 更新 `参考文档/事件与谓词Schema.md` 或新增确认记录。
- 修正事件和谓词规则。
- 重跑流水线和报告。

### 第 6 步：找 C 联调真实数据

目标：确认 C 端能用当前 CSV 读取、生成因子、跑回测，并且未来函数审计通过。

使用文件：

- `查看材料/C联调运行手册.md`
- `查看材料/C联调数据契约清单.md`
- `查看材料/未来函数审计明细.md`
- `查看材料/真实行情校验报告.md`
- `data/sample/event_forward_returns.csv`
- `data/sample/factors.csv`
- `data/sample/backtest_metrics.csv`

给 C 的运行命令：

```bash
.venv/bin/python run_pipeline.py --preserve-inputs
.venv/bin/streamlit run app/main.py --server.port 8501 --server.headless true
```

C 要检查：

1. 所有 CSV 能按字段契约读取。
2. `stock_code` 是 6 位字符串，不带交易所后缀。
3. `event_time` 严格早于 `entry_trade_date`。
4. `event_forward_returns.csv` 只包含完整未来窗口事件。
5. `factors.csv` 中 `future_info_ok` 全为 `true`。
6. Demo 页面能展示最新报告和数据。
7. 如果换行情源，收益、规则、因子、报告全部重刷。

建议联调记录格式：

```text
C 联调记录：
日期：2026-xx-xx
运行命令：
结果：
发现问题：
需要 B 修改：
需要 C 修改：
是否可进入 PPT 数字冻结：是/否
```

## 每完成一批后的固定动作

你每完成一批人工任务后，直接对 Codex 说：

```text
我完成了【谓词抽检/A确认/C联调/PPT数字检查】第 X 批。
请安全复跑、修正必要问题、更新查看材料。
```

Codex 应运行：

```bash
.venv/bin/python run_pipeline.py --preserve-inputs
.venv/bin/python scripts/validate_input_preservation.py
.venv/bin/python scripts/audit_text_sources.py
.venv/bin/python scripts/report_manual_verification_progress.py
.venv/bin/python scripts/validate_real_market_data.py --input data/sample/market_data.csv
.venv/bin/python scripts/validate_manual_review_results.py
.venv/bin/python scripts/validate_b_data.py
.venv/bin/python scripts/validate_research_outputs.py
.venv/bin/python scripts/validate_delivery_package.py
```

## 接下来 90 分钟怎么做

| 时间 | 动作 | 产物 |
|------|------|------|
| 0-10 分钟 | 打开 `谓词人工抽检样本.csv` 和 `事件抽检问题处理记录.md` | 理解修正后的判断边界 |
| 10-45 分钟 | 按 event_id 分组抽检前 20-30 条谓词 | pass/revise/drop 记录 |
| 45-60 分钟 | 把 revise/drop 的具体理由发给 Codex | 谓词规则修正清单 |
| 60-75 分钟 | 把 `A口径确认建议稿.md` 发给 A，要求逐项确认 | A 口径确认记录 |
| 75-90 分钟 | 约 C 按 `C联调运行手册.md` 跑保护模式 | C 联调时间和负责人 |

最优先动作只有一个：先完成谓词抽检。不要再重复核验本轮已经完成的文本、行情决定和事件样本。

## 最终交付前冻结标准

满足以下条件后，可以进入 PPT 数字冻结：

- 真实文本 120 条程序化联网来源核验通过，`源文本核验明细.csv` 无 revise。
- 事件人工抽检 10 条的 `drop` 共性问题已处理完，并有问题处理记录。
- 谓词人工抽检 60 条均为 `pass`，或 `revise/drop` 已处理完。
- 行情口径已记录：接受 `adj_factor=1` 字段占位并披露限制。
- A 已确认金融 Schema 和对外表述边界。
- C 已确认 CSV 读取、收益对齐和回测口径。
- `validate_b_data.py` 输出 `errors=0`。
- `validate_research_outputs.py` 输出 `research_output_errors=0`。
- `validate_delivery_package.py` 输出 `delivery_errors=0`。
- PPT 中所有数字能在 CSV 或查看材料中找到来源。

## 你发给 Codex 的最小信息

每次人工推进后，只需要发这几类信息：

```text
完成批次：谓词抽检第 1 批
谓词：E001 has_policy_support pass；E007 management_response_vague revise 为 false
请按字段契约修正并安全复跑
```

## 边界提醒

- AlphaLens 是量化研究助手，不预测股票价格，不提供投资建议。
- 因子和回测用于研究假设验证，不代表收益承诺。
- 人工确认的价值是保证数据事实、金融语义和展示数字一致。
- 自动脚本可以帮你重跑、校验、更新材料，但 A 的金融口径确认、C 的联调确认和最终 PPT 数字冻结仍需要团队成员完成。
