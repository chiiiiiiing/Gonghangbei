# AlphaLens B 线阶段进展交接单

日期：2026-07-13

## 已完成

- 建立 `data/sample/` 六张 CSV 的可运行样例数据。
- 建立 30 只新能源股票池，覆盖光伏、锂电、风电、储能、整车。
- 建立 120 条摘要化样本文本，覆盖政策、公告、新闻、互动问答，其中 100 条为待人工核验候选摘要。
- 生成实体链接结果：168 行。
- 生成事件抽取结果：168 行。
- 生成谓词判断结果：1848 行，每个事件覆盖 11 个谓词，包含全部 MVP 谓词。
- 生成 2024-01-02 至 2026-06-30 的 30 只股票日频 Demo 行情：19,530 行。
- 写好事件抽取 prompt 与谓词判断 prompt 初版。
- 生成 6 个解释案例草稿，包含原文摘要、事件、谓词和 Demo 5 日收益路径。
- 建立数据质量校验脚本，当前校验结果为 0 个 Fatal errors。

## 自动化命令

```bash
python3 run_b_pipeline.py --skip-sample-generation
python3 scripts/validate_b_data.py
python3 scripts/export_explanation_cases.py
```

说明：真实文本或真实行情开始写入后，只使用保留输入的安全模式；不要使用 `--force-sample-generation`。

## 交付文件

| 文件 | 说明 |
|------|------|
| `data/sample/stock_pool.csv` | 股票池 |
| `data/sample/raw_documents.csv` | 摘要化样本文本 |
| `data/sample/entity_links.csv` | 实体链接结果 |
| `data/sample/events.csv` | 事件抽取结果 |
| `data/sample/predicates.csv` | 谓词判断结果 |
| `data/sample/market_data.csv` | 确定性 Demo 行情 |
| `参考文档/数据来源清单.md` | 数据来源清单与入库原则 |
| `参考文档/事件与谓词Schema.md` | 事件与谓词 schema |
| `参考文档/事件抽取提示词.txt` | 事件抽取 prompt |
| `参考文档/谓词判断提示词.txt` | 谓词判断 prompt |
| `查看材料/数据质量报告.md` | 数据质量报告 |
| `查看材料/解释案例草稿.md` | 解释案例草稿 |

## 当前警告

1. `raw_documents.csv` 当前已达到 100+ 条规模，但 100 条候选摘要仍需人工核验真实来源。
2. `market_data.csv` 是确定性 Demo 行情，只用于本地流程验证；正式回测必须替换为真实前复权行情。

## 给 C 的接口说明

- CSV 字段名严格遵守 `参考文档/数据格式规范.md`，本轮未新增或改名。
- 所有股票代码均为 6 位字符串，无交易所后缀。
- 日期均为 `YYYY-MM-DD`。
- boolean 谓词值均为小写字符串 `true` / `false`。
- 事件日期均不晚于后续 Demo 行情窗口，便于 C 用 `event_time < trade_date` 做未来函数检查。

## 下一步

1. B 用正式来源补齐 100+ 条文本，并把 `raw_documents.csv` 的摘要样例替换成核验版本。
2. B/C 联调真实行情导入，替换 Demo 行情。
3. 用 `.venv/bin/python run_pipeline.py --preserve-inputs` 重跑实体链接、事件、谓词和质量报告。
4. A 从 `查看材料/解释案例草稿.md` 中挑选 3-5 个案例进入 PPT，但需在真实行情替换后刷新收益路径。

本报告仅供研究参考，不构成投资建议
