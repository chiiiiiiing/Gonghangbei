# AlphaLens 宏观 Nowcast 规则路线选择规范

> 状态：协作基线 v1.0
> 适用范围：宏观谓词、月度特征、规则生成路线比较、模型选择与冻结 OOS
> 目标指标：国家统计局“电气机械和器材制造业增加值同比增速”
> 本报告仅供研究参考，不构成投资建议

## 1. 决策结论

AlphaLens 不再把文本输出直接解释为股票涨跌因子。现有文本流水线继续产出可审计的事件、谓词、规则命中和逐股票候选值；新增宏观研究层将这些结果按月聚合为产业景气证据，用于 Nowcast 尚未公布的官方行业指标。

规则路线不凭主观偏好决定。项目必须在同一数据、同一时间切分、同一基础模型和同一评价函数下比较以下路线：

1. `predicate_baseline`：只使用原始谓词和文本统计，不使用组合规则；用于判断规则是否真的增加信息。
2. `historical_rules`：只在训练期从历史文本中学习谓词组合规则，在验证期和 OOS 冻结。
3. `ai_dynamic_rules`：AI 对每篇文本提出宏观传导规则与评分，按月聚合；验证期和 OOS 不允许看到当月尚未公布的官方值。
4. `hybrid_rules`：历史冻结规则为主，AI 只能提出候选或调整文本级证据权重，不能修改冻结规则定义。

默认工程路线为 `historical_rules`，但这不是效果结论。只有完成 2015—2023 历史文本和官方目标覆盖后，才能依据第 9 节协议选择最终路线。如果覆盖不足，系统必须输出“文本预测增量不足”，不得宣布任何文本路线胜出。

## 2. 预测目标与预测时点

目标字段定义如下：

| 字段 | 含义 |
|---|---|
| `period_start` | 统计期起始日 |
| `period_end` | 统计期结束日 |
| `period_kind` | `month` 或 `jan_feb_combined` |
| `target_name` | 固定为 `electrical_machinery_industrial_value_added_yoy` |
| `target_value` | 官方同比增速，单位为百分比，例如 `7.5` 表示 7.5% |
| `release_date` | 国家统计局首次发布日期 |
| `source_url` | 可核验的官方来源链接 |

预测发生在每个统计期末。特征只允许使用 `publish_time <= period_end` 且尚未在更早统计期使用过的当期文本。模型可用的目标历史必须满足 `release_date <= forecast_as_of`。

1—2 月合并为一个独立观测：

```text
period_start = YYYY-01-01
period_end   = YYYY-02-28（闰年为 02-29）
period_kind  = jan_feb_combined
```

禁止拆分或插值成两个官方月度值。

## 3. 时间切分与冻结边界

| 阶段 | 时间 | 允许用途 |
|---|---|---|
| Train | 2015-01-01—2021-12-31 | 宏观规则发现、模型拟合、训练内滚动评估 |
| Validation | 2022-01-01—2023-12-31 | 规则路线、模型和超参数选择 |
| Frozen OOS | 2024-01-01—最新 | 仅最终报告，不得重新选择路线或调参 |

所有选择以 Validation 为准。任何使用 OOS MAE、策略收益或 Oracle 结果反向调整规则、谓词、正则强度、特征集合或仓位参数的行为都属于数据泄漏。

## 4. 谓词分层

现有固定 19 谓词保持不变，继续作为事件与证据层的兼容合同。新增宏观传导谓词使用独立 Schema，避免破坏 `predicates.csv`、冻结回放和现有接口。

### 4.1 宏观传导谓词 Schema

| 谓词 | 经济含义 | 默认方向 | 主要时滞 |
|---|---|---:|---:|
| `policy_expands_effective_demand` | 政策扩大终端或设备需求 | +1 | 1—3 月 |
| `policy_accelerates_project_implementation` | 项目审批、建设或落地提速 | +1 | 1—3 月 |
| `capacity_under_construction` | 产能仍在建设阶段 | +1 | 3—12 月 |
| `capacity_enters_production` | 新产能投产或达产 | +1 | 0—3 月 |
| `order_demand_improves` | 订单、中标或排产改善 | +1 | 0—2 月 |
| `inventory_pressure_increases` | 库存、去库压力上升 | -1 | 0—2 月 |
| `product_price_recovers` | 产品价格或加工费改善 | +1 | 0—2 月 |
| `raw_material_cost_pressure` | 原材料成本挤压生产与利润 | -1 | 0—2 月 |
| `export_demand_improves` | 海外订单或出口需求改善 | +1 | 0—2 月 |
| `grid_investment_accelerates` | 电网投资、招标和设备需求提速 | +1 | 1—6 月 |
| `financing_constraint_eases` | 融资条件改善、资金约束减弱 | +1 | 1—6 月 |
| `industry_competition_intensifies` | 价格战、过剩或竞争加剧 | -1 | 0—3 月 |

每次命中必须同时保存：

- `value`：`true` / `false`；
- `direction`：`-1` / `0` / `1`；
- `intensity`：0—1；
- `confidence`：0—1；
- `expected_lag_months`：非负整数；
- `evidence_text`：必须能逐字回溯到原文；
- `source`：`deterministic`、`ai` 或 `fused`。

没有可回溯证据时不得贡献宏观分数。AI 与确定性程序冲突时沿用保守融合：AI 只能按置信度部分拉动确定性值，不能无证据覆盖。

## 5. 月度特征合同

月度聚合前必须按 `doc_id`、规范化 URL 和近重复文本去重。同一文档映射多只股票时，文档级计数只能贡献一次；股票相关性只能用于加权主题暴露，不能虚增文档支持度。

必备特征组：

1. 覆盖：文档数、独立来源数、事件数、AI 标注成功率。
2. 来源：政策、公告、新闻、互动问答占比，权威来源占比。
3. 原始谓词：19 谓词的命中率、置信度均值和有方向净值。
4. 宏观谓词：12 谓词的加权命中与分时滞暴露。
5. 主题：电池、光伏、储能、电网、风电等独立文档暴露。
6. 规则：触发次数、独立支持文档数、置信度和方向净值。
7. 原候选值摘要：均值、标准差、P25、P50、P75、正值覆盖率。

计数特征使用 `log1p` 或月内占比；连续特征只使用训练期统计量标准化。验证期和 OOS 不得重新估计均值、标准差或缺失值填充值。

## 6. 路线 A：历史学习规则

1. 只使用 Train 文本和当时已经发布的官方目标。
2. 从 1—3 个宏观谓词组成候选规则，至少由 12 个独立文档、6 个独立统计期支持。
3. 同一规则在同一文档只计一次；高度重合的命中文档集合只保留验证前评分更高者。
4. 规则评分只基于训练期滚动预测，不使用一次性全样本拟合值。
5. 稀疏规则的扩展窗在至少 8 个“规则信号与当时已发布目标对齐”的 Train 观测后启动；不足 8 个对齐观测的规则不得评分。该启动值只决定能否做训练内滚动检验，不替代 12 篇独立文档、6 个独立统计期和 72 个 Train 官方目标的最终门槛。
6. 评分组成：方向一致性、目标相关稳定性、跨年度稳定性、独立文档/月份覆盖、证据质量和复杂度惩罚。
7. 进入 Validation 前冻结规则条件、方向、时滞和聚合方式。

禁止沿用当前按未来 5 日股票超额收益挑出的 `rules.csv` 作为宏观规则。它可以作为输入特征或解释参考，但不是宏观路线 A 的训练结果。

## 7. 路线 B：AI 动态规则

AI 对每篇文本最多提出 3 条宏观候选规则，每条必须包含：

- 1—4 个合法宏观谓词条件；
- 方向、强度和预期时滞；
- 原文证据片段；
- 置信度；
- 传导解释；
- `status = pending_macro_validation`。

AI 不接收当月官方目标、未来官方发布日期后的文本、OOS 误差或策略收益。提示词版本、模型身份和结构化输出 Schema 必须冻结并记录。证据不可回溯、谓词非法或模型身份不符时直接拒绝。

动态规则按稳定的“条件签名 + 方向 + 时滞桶”聚合，不能把每次自然语言规则名称直接变成新特征，避免特征空间无限漂移。

## 8. 路线 C：融合规则

融合路线以历史冻结规则为主：

```text
月度融合规则值
= 历史冻结规则值
+ 通过证据门控的 AI 候选值 × AI 权重
```

AI 权重只能在 Train 内选择，并在进入 Validation 前冻结，候选集合为 `{0.0, 0.25, 0.5}`。`0.0` 等价于纯历史路线，可防止无效 AI 强行进入模型。

## 9. 统一模型与路线选择协议

所有路线共享相同的宏观滞后和季节特征。比较模型固定为：

- 持久性基线；
- 季节性基线；
- AR(1)；
- 无文本 Ridge；
- 文本增强 Ridge；
- 文本增强 Elastic Net。

主选择指标为 Validation 相对无文本 Ridge 的增量：

```text
delta_mae  = text_mae  - no_text_mae
delta_rmse = text_rmse - no_text_rmse
delta_r2   = text_oos_r2 - no_text_oos_r2
```

候选路线必须同时满足：

1. Validation `delta_mae < 0`；
2. Validation `delta_rmse < 0`；
3. 景气加速度方向准确率不低于无文本 Ridge；
4. 至少 18 个 Validation 观测有可用预测；
5. 文本特征非零覆盖率至少 60%；
6. 按年度或 6 个月时间块检查时，没有单一时间块贡献超过总误差改善的 60%。

若多条路线合格，依次按 MAE、RMSE、特征数更少、规则更稳定排序。若没有路线合格，最终选择无文本 Ridge，并显示“文本预测增量不足”。

2024 年后的 OOS 只报告 MAE、RMSE、样本外 R²、加速度方向准确率和 90% 区间覆盖率，不改变已冻结选择。

## 10. 数据充足性门槛

在宣称完成路线选择前，必须同时满足：

- Train 官方目标不少于 72 个观测；
- Validation 官方目标不少于 18 个观测；
- Train 和 Validation 文本非空月份覆盖率均不少于 80%；
- 每条入选历史规则满足独立文档和统计期支持门槛；
- 所有官方值均带 `release_date` 和可核验 `source_url`。

当前严格核验文本主要集中在 2024—2026 年，不满足上述条件。因此当前代码只能验证研究协议、数据合同和运行链路，输出必须是：

> 文本预测增量不足

## 11. 策略接口边界

Nowcast 层只向策略输出：预测同比增速、相对最近已公布值的预测加速度、90% 预测区间、预测时点和证据审计。策略不得直接读取 OOS 真实值。

策略固定为新能源 ETF `516160` 的 12 个月时间序列动量 + 60 日波动率缩放 + 宏观确认，防御资产为 `511010`。`399808` 只作为不可交易研究代理。Oracle 必须标记“不可交易，仅作为理论参照上限”。

路线选择阶段不使用策略收益作为主目标，避免为单一交易规则过拟合宏观模型。策略只在宏观路线冻结后做二级验收。

## 12. 协作与变更规则

1. 修改目标字段、时间切分、谓词键、路线选择门槛或未来信息边界前，必须先更新本文档并经团队确认。
2. 现有 19 谓词和 CSV 字段名不得因宏观开发而修改。
3. 新增宏观 CSV 首次落地后字段名视为合同；只能追加可选字段，不得重命名既有字段。
4. 原始官方数据和大规模历史文本放在 `data/raw/` 或 `data/external/`，不得提交 Git；仓库只提交小型可核验样例或空模板。
5. 测试必须覆盖 1—2 月合并、发布日期可用性、文档去重、Train/Validation/OOS 隔离和覆盖不足降级。
6. 任何报告和界面必须保留“本报告仅供研究参考，不构成投资建议”。

## 13. 当前路线比较快照（2026-08-13）

- 300 篇去重文本中，真实 DeepSeek 宏观标注严格通过 296 篇；4 篇因证据不能逐字回溯而保留失败记录。
- 历史路线在 Train 内冻结出 1 条满足支持数和滚动改善门槛的规则；2022—2023 Validation MAE 为 2.383pct。
- AI 动态路线按“条件签名 + 方向 + 时滞桶”形成 17 个规则特征；Validation MAE 为 2.476pct，且没有同时通过全部验证门槛。
- 因此两条规则路线之间暂定优先 `historical_rules`。但 Train 官方目标只有 68 个，低于 72 个门槛，正式选择仍为 `no_text_ridge`，系统必须显示“文本预测增量不足”。
- 补齐缺失的可核验官方历史观测后，按本文协议重跑一次；不得根据2024年后OOS表现修改谓词、规则或门槛。
