# AlphaLens 技术文档

版本日期：2026-08-04

**本报告仅供研究参考，不构成投资建议**

## 1. 产品定位

AlphaLens 是金融文本另类因子研究台。它把政策、公告、新闻和互动问答转换成可追溯的事件、谓词、冻结规则与候选因子，并用历史数据检验研究规则。候选因子是研究排序变量，不是预期收益、目标价或买卖信号。

页面分为：

- **新文本分析**：输入新公开文本，实时调用 DeepSeek，输出逐股票候选因子与证据链。
- **历史研究**：严格分开 Discovery 与 OOS，展示 Rank IC、ICIR、覆盖率、每日五组收益和证据状态。
- **研究审计**：集中披露库存数量、样本分区缺口、人工抽检、行情限制、模型、Prompt、规则和代码版本。

## 2. 系统架构

```text
浏览器
  app/index.html + app/assets/
        ↓ HTTP / JSON
Flask API
  app/server.py
        ├── 模型网关与 Prompt       src/ai/
        ├── 实体、事件、谓词与门控  src/pipeline/
        ├── 透明评分                 src/research/
        ├── 规则发现与回测           src/backtest/
        ├── 外部数据安全导入         src/ingestion/
        └── 样例输入与研究产物       data/sample/
```

前端是浏览器中的研究界面，后端仍是 Python 程序。网页不是把终端画面简单搬过来，而是通过 API 调用同一套 Python 研究函数，并把中间证据和审计状态结构化展示。

## 3. 实时技术路线

```text
新文本
→ BGE/字符 n-gram 检索相似冻结规则
→ DeepSeek V4 Flash 返回逐股票结构化 JSON
→ 事件类型与证据回溯校验
→ 股票池、关系证据与置信度校验
→ 每只股票完整 19 个谓词合法性校验
→ AI 与确定性程序逐项对照
→ 三层一致性门控
→ 冻结规则匹配与同类规则去重
→ 候选因子、公式和自动研究记录
```

实时模式必须有 AI 成功参与。缺少 Key、网络失败、模型身份不符、JSON 无法修复、事件或证据非法、没有逐股票 19 谓词时直接报错，不退化成“假装 AI”的纯规则结果。冻结回放是独立的断网入口，始终标记 `is_replay=true`。

## 4. DeepSeek 身份和结构化输出

调用模型固定为 `deepseek-v4-flash`，基础地址固定为 `https://api.deepseek.com`。

`POST /api/ai/check` 执行两步：

1. 调用 `GET /models`，确认当前 Key 的模型列表包含 `deepseek-v4-flash`，记录 `owned_by`。
2. 发起最小 `POST /chat/completions`，要求返回 JSON，并检查响应 `model` 必须与请求模型完全相同。

检查结果记录请求模型、实际模型、请求 ID、`system_fingerprint` 和 API 主机；不返回或保存 Key。401、402、403、429 分别显示 Key 无效、余额不足、权限不足和限流原因。即使用户跳过“检查连接”直接分析，正式结果也会再次核对实际返回模型。

Prompt 版本为 `alphalens-research-v2.0`。核心输出为 `stock_analyses`，每只股票分别返回：

- 股票代码和名称。
- 可在原文连续定位的关系证据与置信度。
- 证据回溯、信息具体性和业务关联三个辅助评分。
- 完整、无重复的 19 个谓词及其置信度和理由。

首次输出校验失败时，系统把具体错误反馈给模型修复一次；第二次仍失败才终止。

## 5. 三层一致性门控

### 5.1 事件门控

确定性程序和 AI 同时识别事件类型时必须一致。确定性规则未覆盖、但 AI 事件枚举合法且证据可回溯时，状态为 `ai_validated`；两者冲突或证据不可回溯时不生成因子。

### 5.2 实体关系门控

股票代码必须属于锁定股票池。AI 的关系证据必须是输入原文的连续子串，关系置信度不得低于 0.50。确定性实体映射保留用于解释，但没有通过 AI 关系校验的股票不能触发因子。

### 5.3 谓词门控

| 状态 | 含义 | 是否可触发规则 |
|---|---|---|
| `agreed_true` | AI 与确定性程序均为真 | 是 |
| `agreed_false` | 两者均为假 | 否 |
| `disputed` | 两者不一致 | 否 |
| `invalid` | 缺失、重复、非法或越界 | 否 |

两个数值谓词的差值不超过 0.10 才视为一致；数值谓词只参与因子倍率，不会被当成布尔条件。AI 新提议规则统一标记 `pending_statistical_validation`，不能直接进入正式因子。

## 6. 透明证据评分

事件证据分不再按来源写死一个总分，而拆成：

```text
E = 0.30 × 来源可靠性
  + 0.25 × 证据回溯
  + 0.25 × 信息具体性
  + 0.20 × 业务关联
```

- 来源可靠性：政策、交易所或正式公告 0.95；互动问答 0.85；主流财经媒体 0.80；其他新闻 0.60。
- 证据回溯：证据句能在原文中连续定位为 1，否则为 0。
- 信息具体性：行动、明确主体、量化事实、日期或项目事实四项等权。
- 业务关联：公司名称直接出现为 1；产业主题映射 0.80；宽主题代表股票映射 0.60。

AI 只辅助后三项。最终每项取“确定性结果与 AI 结果的较小值”，因此 AI 不能把证据分抬高，只能提供保守校正。页面逐项展示分数和权重。

## 7. 数值影响谓词

`event_has_short_term_price_impact` 不再由 AI 或人工直接给固定值。系统只使用 Discovery，同类事件按独立 `doc_id` 聚合行业超额收益，统计绝对 5 日行业超额收益达到 2% 的文档数，并使用 Beta(2,2) 先验：

```text
影响后验概率 = (命中文档数 + 2) / (独立文档数 + 4)
```

结果写入 `event_type_impact_priors.csv`。该概率表示历史同类事件出现较大短期波动的后验频率，不代表上涨概率或收益预测。

## 8. 规则发现与透明评分

规则发现只使用 2024-01-01 至 2025-12-31 的 Discovery 数据。支持度按独立文档计算，并同时记录独立日期和股票覆盖；一篇行业政策映射多只股票仍只增加一份独立支持。事件集合高度重复的规则只保留最高分，同一目标标签触发时也只保留最高分规则。

规则分数为：

```text
S = 0.30 × Beta 后验胜率
  + 0.25 × 收缩收益分位
  + 0.20 × 半年稳定性
  + 0.15 × 文档/股票覆盖
  + 0.10 × 平均证据分
  - 0.03 × (谓词项数 - 1)
```

- 后验胜率使用 Beta(2,2)。
- 平均行业超额收益按样本量向全局 Discovery 均值收缩，再用 Logistic 转为 0 到 1 分量。
- 半年稳定性是正收益半年数占有样本半年数的比例。
- 覆盖项由最多 20 篇文档和 10 只股票共同构成。

所有分项写入 `rule_diagnostics.csv` 并在审计页展示。

## 9. 因子公式

```text
候选因子 = 门控后冻结规则评分和
           × (0.7 × 透明证据分 + 0.3 × 同类事件影响后验)
```

事件因子先聚合到唯一“交易日 + 股票”，再按行业做截面标准化。候选因子只表示当前研究规则对文本证据的响应强度。

## 10. 历史回测

- Discovery：2024-01-01 至 2025-12-31，只用于发现和评分。
- OOS：2026-01-01 至 2026-06-30，只用于样本外检验。
- 收益：入场日开盘至第 5 个交易日收盘的股票收益，减同日行业等权收益。
- 五组：每个交易日独立按横截面因子排序分组，不全局排序事件。
- Rank IC：每日因子排序与随后 5 日行业超额收益排序的 Spearman 相关。
- ICIR：有效日 Rank IC 均值除以标准差。
- 未来函数：入场交易日必须严格晚于 `event_time`。

当前 OOS 只有 1 个因子活跃日、0 个有效 IC 日，必须显示“证据不足”；既不能证明稳定有效，也不能判断因子已经失效。

## 11. Embedding 检索

优先模型为 `BAAI/bge-small-zh-v1.5`。模型只从本机缓存加载，除非显式设置 `ALPHALENS_BGE_ALLOW_DOWNLOAD=1`。未安装依赖或模型未缓存时，系统降级到自主实现的确定性字符 2/3/4-gram 哈希向量，并在结果和审计中标记真实后端，不能把 n-gram 冒充语义模型。

## 12. 历史 AI 缓存

`批量生成AI标注.py` 逐篇调用 DeepSeek，缓存键包含 `doc_id + 文档哈希 + Prompt 版本 + 模型名`。输出 `ai_annotations.jsonl`，支持跳过成功记录和重试失败记录。缓存保存结构化结果、模型、请求 ID、指纹和校验状态，不保存 API Key。

## 13. 真实数据安全导入

### 文本

`导入真实文本.py` 要求采集清单严格使用锁定七字段。程序检查 `doc_id`、来源类型、日期、详情页 URL、重复 URL、标题和正文长度，默认只生成 `data/external/文本导入暂存/` 报告。只有 `--apply` 才按 `doc_id` 更新或追加 `raw_documents.csv`。

### 行情

`更新真实行情.py` 通过 AkShare 请求股票池前复权日线，检查 30 只股票均有数据后写入暂存区。只有 `--apply` 才替换 `market_data.csv`。按照已确认限制，价格列使用前复权口径，`adj_factor=1` 保留为占位字段并持续披露。

当前样本分区不均衡：Discovery 公告和互动问答不足，OOS 政策和新闻不足。系统在审计页按“分区 × 来源类型”展示目标 25 篇及待补数量，不通过复制文档虚增独立支持。

## 14. API

| 方法 | 路径 | 用途 |
|---|---|---|
| `POST` | `/api/analyze` | 实时 AI 分析 |
| `POST` | `/api/ai/check` | Key、模型列表和实际模型身份检查 |
| `GET` | `/api/replay/<case_id>` | 冻结回放，强制 `is_replay=true` |
| `GET` | `/api/backtest` | Discovery、OOS 与历史审计指标 |
| `GET` | `/api/audit` | 数据分布、版本、缓存、限制和规则诊断 |

分析响应包含 `event_consensus`、`entity_consensus`、`predicate_consensus`、`disputed_predicates`、`consensus_gate_passed` 和逐股票 `evidence_score_breakdown`。锁定 CSV 字段未修改；新增统计放在独立诊断文件中。

## 15. 输入保护与已知限制

`运行研究流水线.py` 只重建实体、事件、谓词、规则和回测产物。它不生成样本文本，并强制比较重算前后 `raw_documents.csv` 的 SHA-256。

已知限制：

- 当前历史 AI 缓存需要队伍使用自己的 DeepSeek Key 批量生成。
- BGE 是可选依赖；未准备模型时使用可复现的字符 n-gram 降级。
- 当前样本分区不平衡、OOS 有效日期不足。
- `adj_factor=1` 不是真实复权因子序列。
- 回测未完整计入交易成本、流动性和做空约束。

**本报告仅供研究参考，不构成投资建议**
