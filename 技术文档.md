# AlphaLens 技术文档

版本日期：2026-08-04

**本报告仅供研究参考，不构成投资建议**

## 1. 定位与架构

AlphaLens 是金融文本量化研究助手，不预测股票价格，不输出买卖建议。

```text
浏览器 app/index.html
        ↓ HTTP / JSON
Flask API app/server.py
        ├── AI 研究层 src/ai/
        ├── 文本分析层 src/pipeline/
        ├── 规则与回测 src/backtest/
        └── 演示数据 data/sample/
```

页面分为“新文本分析”“历史研究”“研究审计”。首页只放输入、运行配置和结果；数据数量、版本和限制集中到审计页。

## 2. 实时 AI 路线

```text
新文本
→ 字符 n-gram Embedding 检索相似冻结规则
→ DeepSeek V4 Flash 返回结构化 JSON
→ 程序校验股票、事件、证据和 19 个谓词
→ AI 谓词与确定性谓词逐项对照
→ 一致性门控
→ 冻结规则匹配
→ 候选因子与自动研究记录
```

DeepSeek 使用 OpenAI 兼容的 `/chat/completions` 接口和 `deepseek-v4-flash` 模型。页面 Key 只在当次 HTTP 请求中使用，服务端不持久化。`POST /api/ai/check` 用最小 JSON 请求同时检查 Key 有效性和模型权限，并区分无效 Key、余额不足、权限不足、限流和网络故障。Prompt `alphalens-research-v1.1` 提供完整 JSON 字段模板；当可解析结果未通过事件枚举、证据回溯或 19 个谓词完整性校验时，系统会把明确错误反馈给模型自动修复一次，第二次仍不合格才终止分析。

## 3. 一致性门控

模型必须返回锁定 Schema 中的全部 19 个谓词；缺失、重复、非法枚举、数值越界或证据无法在原文中连续定位时拒绝该次输出。

| 状态 | 含义 | 是否可触发规则 |
|---|---|---|
| `agreed_true` | AI 与确定性程序均为真 | 是 |
| `agreed_false` | 两者均为假 | 否 |
| `disputed` | 两者不一致 | 否 |
| `invalid` | 缺失或非法 | 否 |

数值谓词在差值不超过 0.10 时记为一致，但它们不会被当作布尔条件触发规则。AI 自动提出的规则始终标记为“待统计验证”，不直接进入正式因子。

## 4. 文本、事件和谓词

历史文本是规则发现的证据库。每篇文本先关联股票，再抽取事件与证据句，最后转换成可计算谓词。

- 互动问答在 7 日内同一股票至少 3 条，且相对之前 30 日基线明显上升时，才形成投资者问询压力事件；单条问答只作证据。
- 新闻关注度按行业过去 3 日文本频率相对之前 20 日基线判定，不再由单篇关键词直接触发。
- 政策映射多只股票时，规则支持数按独立 `doc_id` 计一次。

## 5. 规则与因子

规则发现只使用 2024-01-01 至 2025-12-31 的 Discovery 数据。一条规则需要同时满足独立文档、独立日期和股票覆盖要求；高度重复的同类规则只保留评分最高者参与因子。

新文本因子公式为：

```text
候选因子 = 门控后冻结规则评分和
           × (0.7 × 事件证据强度 + 0.3 × 事件短期影响先验)
```

该值用于研究排序和证据追溯，不是预期收益、目标价或交易信号。

## 6. 回测口径

- Discovery：2024-01-01 至 2025-12-31，只用于规则发现和评分。
- OOS：2026-01-01 至 2026-06-30，只用于样本外检验。
- 收益标签：股票 5 日收益减同日行业等权 5 日收益。
- 因子截面：事件信号先聚合到唯一“交易日 + 股票”，再行业内标准化。
- 五组收益：每个交易日独立横截面分组，不全局排序事件行。
- Rank IC：每日因子排序与随后 5 个交易日行业超额收益排序的 Spearman 相关。
- 因子覆盖率：有因子值的唯一股票日占当期可用行情股票日的比例。
- 衰减提示：OOS 有效 IC 日不足 20 时输出“无法判断衰减”，不将稀疏样本误判为因子失效。
- 时间边界：`event_time` 必须早于实际收益入场日，因子日不使用未公开信息。

当前 OOS 只有少量因子样本且没有足够的有效 Rank IC 日，系统明确输出“证据不足”，不宣称因子稳定有效。

## 7. API

| 方法 | 路径 | 用途 |
|---|---|---|
| `POST` | `/api/analyze` | 实时 AI 分析；请求格式保持兼容 |
| `POST` | `/api/ai/check` | 检查 Key 和模型权限，不保存 Key |
| `GET` | `/api/replay/<case_id>` | 冻结回放，强制返回 `is_replay=true` |
| `GET` | `/api/backtest` | 返回 `discovery`、`oos` 和 `audit` 分区 |
| `GET` | `/api/audit` | 数据、版本、审计和规则支持诊断 |

`POST /api/analyze` 响应包含 `predicate_consensus`、`disputed_predicates` 和 `consensus_gate_passed`。新增诊断 CSV 为 `rule_diagnostics.csv`、`backtest_metrics_by_split.csv` 和 `group_returns_by_split.csv`，未修改任何锁定 CSV 字段名。

## 8. 重算与输入保护

`运行研究流水线.py` 依次重建实体链接、事件、谓词和历史研究输出，它不包含样本文本生成步骤。运行前后对 `raw_documents.csv` 计算 SHA-256，哈希不一致时直接报错。

## 9. 已知限制

- `adj_factor=1` 是已接受的占位字段，不是真实复权因子序列。
- 当前样本与合格规则数量有限，统计用于验证研究链路。
- 回测未完整建模交易成本、流动性和做空约束。
- 本地字符 n-gram Embedding 为轻量可复现检索，不等同于专用中文语义向量模型。
- 冻结回放是明确标记的演示备用，不是实时模型调用。

**本报告仅供研究参考，不构成投资建议**
