# AlphaLens 技术文档

版本日期：2026-08-03

本报告仅供研究参考，不构成投资建议

## 1. 项目定位

AlphaLens 是金融文本量化研究助手，将政策、公告、新闻和互动问答转化为可追溯的实体、事件、谓词、规则与另类因子。系统不预测股票价格，不输出买卖建议。

## 2. 系统架构

```text
浏览器 app/index.html
        ↓ HTTP
Flask API app/server.py
        ├── AI 研究层 src/ai/
        ├── 文本分析层 src/pipeline/
        ├── 因子回测层 src/backtest/
        └── 历史数据 data/sample/
```

- `app/`：提供页面、API 和本地 Plotly 图表。
- `src/ai/`：管理 DeepSeek 请求、Prompt、结构化 JSON 校验和 Embedding 检索。
- `src/pipeline/`：负责实体链接、事件抽取、谓词判断和新文本在线分析。
- `src/backtest/`：负责规则搜索、因子计算、收益对齐、分组回测和 Rank IC。
- `data/sample/`：保存演示所需的文本、行情、规则、因子和回测结果。

## 3. 新文本分析路线

模式一固定执行：

```text
新文本
→ Embedding 检索相似冻结规则
→ DeepSeek V4 Flash 返回结构化 JSON
→ 程序校验股票、事件、证据和谓词
→ AI 判断与确定性谓词对照
→ 冻结规则匹配
→ 候选因子
```

### 3.1 Embedding 检索

本地 `local-char-ngram-embedding-v1` 将新文本和冻结规则映射为 512 维字符 n-gram 向量，通过余弦相似度返回最接近的规则。稳定哈希保证同一输入可复现。

### 3.2 大模型结构化输出

页面填写的 DeepSeek API Key 只随单次请求发送，不写入浏览器存储、CSV 或仓库。模型按 JSON Schema 返回事件、相关股票、谓词和候选规则。

模式一缺少 Key、API 调用失败或 JSON 不合法时返回错误，不生成候选因子，也不伪装成 AI 成功结果。

### 3.3 程序校验

程序检查股票池、事件枚举、谓词名称、布尔值、数值范围和原文证据。非法股票、重复结果和无法回溯到正文的证据会被拒绝。

AI 结果不会直接覆盖冻结规则。候选规则状态保持为“待统计验证”，只有完成历史统计和人工金融口径审核后才能进入正式规则库。

## 4. 历史研究流程

```text
raw_documents.csv
→ entity_links.csv
→ events.csv
→ predicates.csv
→ predicate_matrix.csv
→ event_forward_returns.csv
→ rules.csv
→ factors.csv / factor_snapshot.csv
→ backtest_metrics.csv
```

历史文本用于建立事件和谓词样本，判断哪些可解释组合在历史研究区间具有足够支持。当前规则进入冻结库至少需要 5 次历史触发。

## 5. 谓词与因子

谓词是可以被程序验证的金融命题，例如政策支持、核心产品相关、权威来源、需求侧政策、风险披露或关注扩散。布尔谓词采用 `true/false`，强度类谓词采用限定范围数值。

新文本候选因子由触发规则评分、事件证据强度和事件短期影响先验共同构成。页面同时展示规则编号、谓词条件和证据文本，保证结果可追溯。

候选因子只表示研究信号，不表示预期收益、目标价格或交易建议。

## 6. 回测边界

- 因子日只能使用当日及之前公开的信息。
- `event_time` 必须早于实际收益入场日期。
- 规则发现区间和样本外测试区间分离。
- 前向收益按交易日期对齐，不按原始行号移动。
- 新输入文本不会写入历史 CSV，也不会即时获得未来收益。

页面展示冻结规则、最新因子截面、分组收益、累计收益和 Rank IC。Rank IC 先把事件信号聚合为唯一的“交易日 + 股票”因子，未触发股票补零，再在行业内标准化，并与 30 只股票的 5 日前向收益计算 Spearman 秩相关。所有历史指标直接读取 `data/sample/` 中的研究结果，不使用手填数字。

## 7. 数据规模

当前演示数据包括：

- 30 只新能源股票。
- 130 条金融文本。
- 461 条实体链接。
- 210 个结构化事件。
- 3990 条谓词判断。
- 12 条冻结规则。
- 167 条历史因子样本。
- 18002 行行情记录。

## 8. 运行方式

```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
.venv/bin/python 启动演示.py
```

默认地址为 `http://127.0.0.1:8701`。页面运行所需 Plotly 已存放在 `app/vendor/`，不依赖外部 CDN。

## 9. 局限

- `adj_factor=1` 是已接受的字段占位，不是真实复权因子序列。
- 当前样本规模有限，统计结果用于研究链路验证。
- 回测未完整建模交易成本、流动性和做空约束。
- 本地字符 n-gram Embedding 是轻量可复现检索，不等同于专用中文语义向量模型。
- 模型输出仍可能存在误判，因此必须保留程序校验和人工审核。

本报告仅供研究参考，不构成投资建议
