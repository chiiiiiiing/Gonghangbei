# AlphaLens V6 工行杯现场演示操作文档

> 适用版本：`lithium-v6-quality-text-walkforward`
>
> 本报告仅供研究参考，不构成投资建议。

## 1. 演示目标

现场主演示围绕碳酸锂 V6 质量规则文本 Alpha，讲清楚三件事：

1. 产业文本如何转换为 17 个固定谓词和未来 5 日方向分数；
2. V6 质量门禁如何只在“权威、非交易所、足够偏多、非高不确定性”时增加仓位；
3. 历史 walk-forward 增量与严格前瞻账本如何分离，为什么不能把历史回测冒充成严格 OOS。

## 2. 现场启动

```bash
cd "/你的路径/工行杯_AlphaLens_v6"
python3 -m venv .venv
.venv/bin/python -m pip install -r requirements.txt
.venv/bin/python 启动演示.py
```

浏览器打开：

```text
http://127.0.0.1:8701/
```

建议优先使用“研究验证”页做稳定演示；实时 AI 仅在网络和 DeepSeek Key 可用时加演。

## 3. 推荐演示顺序

### 第一步：首页与两阶段合约

1. 展示“新文本预测”首页。
2. 说明第一段是文本到方向分数，第二段是质量规则到 V5/V6 增量仓位。
3. 强调系统不直接买卖，不预测个股；输出是研究证据和相对成熟基准的仓位边际。

### 第二步：研究验证页

1. 点击“研究验证”。
2. 找到 V6 优化候选卡片。
3. 展示 2025+ 历史滚动年化收益差、Bootstrap 95% 下界、2026 压力段收益差和下界。
4. 说明 V6 的质量规则是 `authoritative_source AND non_GFEX AND zero_shot_score >= 0.2 AND NOT uncertainty_high`，文本权重为 0.35。
5. 指出严格前瞻账本只有 1 条记录、0 条结算，因此系统明确显示“严格前瞻交易增量待检验”。

### 第三步：实时 AI 加演

1. 回到“新文本预测”。
2. 输入一篇可公开发送的碳酸锂相关公告或政策文本。
3. 填写 DeepSeek Key，先点击“检查连接”。
4. 确认返回模型为 `deepseek-v4-flash`。
5. 点击“开始分析”。
6. 展示 V5 和 V6 两套质量映射，说明 V6 门槛更高，因此同一文本可能在 V5 激活、在 V6 不激活。

## 4. 关键数据口径

| 项目 | 口径 |
|---|---|
| 基准 | 20/60/120 日同合约动量 + 40 日已实现波动率 + 25% 年化波动率目标 |
| V6 文本门禁 | 权威来源、非广期所仓单、零样本分数 ≥ 0.2、非高不确定性 |
| V6 文本权重 | 0.35 |
| 主成本 | 单边 5 bp，敏感性覆盖 2/10 bp |
| 严格验收 | append-only 账本达到 63 个已结算交易日，且 Bootstrap 95% 下界大于 0 |

## 5. 上场前一分钟检查

- [ ] `启动演示.py` 已启动，`http://127.0.0.1:8701/` 可打开。
- [ ] 研究验证页可以看到 V6 卡片。
- [ ] V6 截图和录屏已替换为新版。
- [ ] 如果演示实时 AI，Key 已单独验证且不写入任何文件。
- [ ] 浏览器不展示 API Key、本地路径或未公开数据。

本报告仅供研究参考，不构成投资建议。
