# AlphaLens 工行杯现场演示操作文档

> 适用版本：`AlphaLens-Demo-v2.0-R1`
>
> 本报告仅供研究参考，不构成投资建议

## 1. 演示前准备

工行杯成果目录已经直接放置源码、样例数据、截图和视频，**不需要解压任何 ZIP**。建议携带一台已完成依赖安装的电脑，并提前确认：

- Python 3.9 或更高版本；
- Chrome、Edge 或 Safari 浏览器；
- 工行杯成果目录完整，至少包含 `app/`、`src/`、`data/sample/`、`requirements.txt`、`启动演示.py`；
- 如需演示实时 AI，现场网络可访问 DeepSeek API，并准备有效 Key；
- Key 不要写入脚本、README、环境文件或演示截图。

冻结回放不需要联网，也不需要 API Key，适合作为现场的稳定主演示。实时 AI 用于证明新文本会被真实模型处理，建议作为联网正常时的加演环节。

## 2. 第一次在新电脑安装

打开“终端”，进入成果目录。路径中有中文或空格时务必使用引号：

```bash
cd "/你的路径/工行杯_AlphaLens"
python3 --version
python3 -m venv .venv
.venv/bin/python -m pip install --upgrade pip
.venv/bin/python -m pip install -r requirements.txt
```

依赖只需安装一次。安装生成的 `.venv/` 仅留在演示电脑中，不应重新放入提交材料或 Git。

## 3. 每次现场启动网页

在终端执行：

```bash
cd "/你的路径/工行杯_AlphaLens"
.venv/bin/python 启动演示.py
```

看到服务启动提示后，在浏览器地址栏打开：

```text
http://127.0.0.1:8701/
```

首页标题应为“新文本预测”，页面应展示文本输入、正文链接、来源、日期与运行模式。不要直接双击 `index.html`；应用需要本地 Python 服务提供 API 和样例数据。

## 4. 推荐的 4 分钟现场演示顺序

### 第一步：说明产品目标（约 30 秒）

说明 AlphaLens 不预测股票涨跌。它把政策、公告、新闻和互动问答转换为可审计的结构化证据，预测国家统计局“电气机械和器材制造业增加值同比增速”，再用预先确定的量化策略检验预测数据是否提供增量。

### 第二步：冻结回放展示完整链路（约 2 分钟）

1. 在首页选择“冻结回放”。
2. 载入内置样例并点击分析按钮。
3. 指出页面始终显示“冻结回放”，没有冒充实时 DeepSeek 请求。
4. 展示顶部的同比预测值、90%区间和相对最新已公布值的加速度。
5. 向下展开 AI 与确定性事件对照、逐股票关系证据、固定19谓词、三层门控和冻结规则。
6. 继续滚动到页面末尾，展示新能源ETF的12个月趋势、60日波动率缩放与AlphaLens宏观确认，以及买入持有、纯趋势、已公布宏观、AlphaLens和不可交易Oracle五条收益曲线。
7. 说明单篇文本只贡献本次结构化特征，不代表它能独立解释整个行业；结果是研究证据，不是投资建议。

### 第三步：研究验证与防未来数据（约 60 秒）

1. 点击“研究验证”。
2. 展示训练期 2015—2021、验证期 2022—2023 与冻结观察期 2024—最新。
3. 指出每条特征有公开日期，训练特征必须在目标发布日期之前可得；1—2月官方合并值不拆分。
4. 展示历史目标、模型误差、数据覆盖、AI缓存、冻结规则和未来函数审计。
5. 说明模型验证和研究审计已合并在一个页面，便于一次核对研究结论与证据边界。

### 第四步：可选的实时 AI（网络正常时，约 60 秒）

1. 返回“新文本预测”，切换到“实时 AI”。
2. 输入一篇可以公开发送的政策、公告、新闻或互动问答正文；也可以填写公开 HTML/PDF 正文链接。
3. 输入 DeepSeek Key，先点击连接检查。
4. 检查结果应显示请求模型和实际返回模型均为 `deepseek-v4-flash`，并带有 `request_id` 与 `system_fingerprint`。
5. 点击开始分析。完整 19 谓词或证据校验失败时系统会明确报错，不会回退成伪 AI 结果。
6. 演示结束后清空输入框。Key 只随当次请求发送，不写入项目或浏览器存储。

## 5. 量化策略讲解口径

页面末尾策略是验证宏观预测是否有金融研究增量，不是让 AI 直接决定买卖：

- 风险资产：新能源 ETF `516160`；防御资产：5年期国债 ETF `511010`；
- 月末计算信号，下一交易日调仓；
- 12个月趋势不为正时新能源仓位为0；
- 趋势为正且AlphaLens预测加速度为正时使用完整波动率缩放仓位，预测加速度不为正时减半；
- 60日波动率缩放至10%目标年化波动率，仓位不超过100%，不做空、不加杠杆；
- 主回测扣除单边10bp成本，并比较买入持有、纯趋势、趋势+已公布宏观、趋势+AlphaLens和Oracle；
- Oracle 使用下一期真实值，只是不可交易上限，不能用于宣传或调参。

现场应强调：预测准确但策略没有改善，只能证明产业研究价值；预测不准但策略偶然改善，也不能认定成功。

## 6. 关闭应用

回到运行服务的终端，按：

```text
Control + C
```

终端回到命令提示符即表示服务已停止。关闭浏览器标签页即可。

## 7. 常见问题排查

### 7.1 浏览器打不开 127.0.0.1:8701

先确认终端中的服务进程仍在运行。可以另开一个终端检查端口：

```bash
lsof -nP -iTCP:8701 -sTCP:LISTEN
```

如果 8701 被其他程序占用，可以改用 8702：

```bash
ALPHALENS_DEMO_PORT=8702 .venv/bin/python 启动演示.py
```

随后打开 `http://127.0.0.1:8702/`。

### 7.2 提示找不到 Flask 或其他模块

说明虚拟环境尚未安装完整依赖，重新执行：

```bash
.venv/bin/python -m pip install -r requirements.txt
```

必须使用 `.venv/bin/python` 启动，避免调用到另一套 Python 环境。

### 7.3 页面仍显示旧版本

先确认 `VERSION` 内容为 `AlphaLens-Demo-v2.0-R1`，再在浏览器强制刷新：

- macOS：`Command + Shift + R`
- Windows：`Ctrl + F5`

如果仍未更新，停止服务后重新启动。

### 7.4 冻结回放能用，实时 AI 失败

冻结回放和实时 AI 是两条明确区分的演示模式。实时 AI 失败时依次检查网络、Key 权限、DeepSeek 服务状态和模型权限。系统固定要求 `deepseek-v4-flash`，返回其他模型或缺少完整结构化结果会被拒绝，这是预期的安全行为。

### 7.5 现场是否需要运行研究流水线

不需要。现场演示只运行 `启动演示.py`。如果确需重建派生数据，唯一允许的命令是：

```bash
.venv/bin/python 运行研究流水线.py
```

该入口会在重算前后校验 `raw_documents.csv` 的 SHA-256，防止覆盖人工核验成果。

## 8. 上场前一分钟检查表

- [ ] 成果目录直接包含源码，不依赖 ZIP。
- [ ] `.venv` 已在演示电脑本地建好且依赖安装完成。
- [ ] `http://127.0.0.1:8701/` 可以打开。
- [ ] 冻结回放可以完整跑到同比预测与策略回测。
- [ ] “研究验证”页面可以打开，图表没有遮挡或横向溢出。
- [ ] 如演示实时 AI，网络和 Key 已单独验证，输入文本可以公开发送。
- [ ] 桌面准备好本地录屏，网络失败时可立即播放 `演示视频/AlphaLens_演示录屏.webm`。
- [ ] 不展示 Key、个人目录、临时文件或未公开数据。

---

AlphaLens 预测行业实体景气，不预测股票价格，不提供买卖建议。

**本报告仅供研究参考，不构成投资建议**
